# 비트스트림 규격 v0 (2026-08-21)

지위: **초안이자 굳히지 않은 문법이다.** v0는 "인코더가 낸 것을 디코더가 정수로 정확히 되돌리는가"를 확인하기 위한 최소판이다. 압축률로 남과 겨루는 판이 아니다.

이름은 **ingot**이다. 시그니처 네 글자는 `INGT`, 파일 확장자는 `.igt`, 코드 접두사는 `ingot_`이다.

## 왜 이 순서로 만드나

실제로 살아남은 코덱 프로젝트들의 개발 순서를 조사해 그대로 따랐다. 인코더와 디코더를 같은 루프에 넣고, 정수만 쓰고, 문법을 문서에 못 박고, 고정 결과 파일로 잠근다. 최적화·가변 블록은 미룬다. 새 포맷을 만든 프로젝트들은 첫 스트림을 인코딩한 날 디코딩까지 했고, 가장 빠른 디코더로 알려진 구현조차 어셈블리보다 C 완성을 먼저 끝냈다.

한 가지만 그 순서를 벗어난다. **조각 독립 구조를 v0부터 넣는다.** 이것은 나중에 못 끼워 넣는 문법의 뼈대이기 때문이다. 기존 표준에서 화면을 슬라이스로 나누면 압축률이 평균 8%, 타일이면 3.7%, 파면 병렬이면 1.1% 깎인다. 손해 없이 병렬화한 설계는 처음부터 조각을 독립시켰고, 스레드 수를 비트스트림에서 뺐다.

## 여덟 축 중 v0가 이미 담는 것

| 축 | v0에서 |
|---|---|
| 결정성 | 정수 연산만. 부동소수점 금지. 인코더가 디코더와 같은 복원 화소를 본다 |
| 병렬성 | 조각 독립 + 목차. 스레드 수가 파일에 안 실린다 |
| 단순성 | 아래 예산을 잠근다 |
| 특허 자유 | 만료 기술만 사용, 새 도구 없음 |

나머지 넷(압축률·인코딩 속도·디코딩 속도·범용성)은 v0의 목표가 아니다.

## 단순성 예산 (먼저 잠근다)

- 디코더 핵심 코드: **1,500줄 이하** (주석·시험 제외)
- 이 문서: **10쪽 이하**
- 외부 의존: **0**
- 부동소수점 연산: **0개**

예산을 넘기면 기능을 넣지 말고 이 문서를 먼저 고친다.

## 데이터 모델

- 입력: 8비트 RGB, 알파 없음, 색 관리 없음
- 내부: Y·Cb·Cr 세 평면, 8비트, 서브샘플 없음(4:4:4)
- 좌표: 왼쪽 위가 (0,0)

## 파일 구조

모든 정수는 **리틀엔디언**이다.

### 헤더 (고정 24바이트)

| 오프셋 | 크기 | 이름 | 뜻 |
|---|---|---|---|
| 0 | 4 | signature | `INGT` (0x49 0x4E 0x47 0x54) |
| 4 | 1 | version | 0 |
| 5 | 1 | flags | 아래 |
| 6 | 1 | quality | 0~63. 클수록 거칠다. 62에서 화소당 0.1비트 아래로 내려간다 |
| 7 | 1 | group_log2 | 조각 한 변의 로그2. 6~10 (64~1024). v0 기본 8 |
| 8 | 4 | width | 1 이상 65535 이하 |
| 12 | 4 | height | 1 이상 65535 이하 |
| 16 | 4 | group_count | 조각 개수. width·height·group_log2로 계산한 값과 **반드시 같아야 한다** |
| 20 | 4 | hash32 | 목차와 조각 데이터 전체의 FNV-1a 32비트 해시. 아래 「손상 감지 해시」 |

**flags 바이트**

| 비트 | 이름 | v0 |
|---|---|---|
| 0 | colorspace | 0 = BT.601 정수 근사. 1은 예약 |
| 1 | chroma_subsample | 0 = 4:4:4, 1 = 4:2:0 |
| 2 | loop_filter | 1 = 디코더가 블록 경계 필터를 돌린다 (아래 「블록 경계 필터」) |
| 3~7 | reserved | **전부 0이어야 한다. 하나라도 1이면 디코더는 거절한다** |

예약 자리를 "무시하라"가 아니라 **"1이면 거절"**로 정한 이유: 나중에 이 비트로 뜻이 바뀌는 기능을 넣을 수 있게 하려는 것이다. 무시하라고 정해 두면 옛 디코더가 새 파일을 조용히 잘못 그린다.

비트 2 가 그 자리를 실제로 쓴 첫 사례다. 필터를 켠 파일을 옛 디코더에 주면 예약 비트 규칙에 걸려 **깨끗이 거절**된다(실측: `decode failed: reserved bit set`).

반대 방향은 이 비트만으로는 안 된다. 필터를 넣은 판에서 계수 부호화도 함께 바뀌었으므로, v1.6 파일을 v1.7 디코더에 주면 머리말 해시에 걸려 거절된다(실측: `decode failed: malformed group data`). **양쪽 다 거절이고 조용한 오답은 없다.** 비트 2 가 「필터만 바꾼 판」을 만들 때 옛 파일을 읽게 해 주는 것은 사실이지만, 이번 판이 그런 판은 아니다.

### 목차 (조각마다 4바이트)

헤더 바로 뒤에 `group_count`개가 이어진다.

| 크기 | 이름 | 뜻 |
|---|---|---|
| 4 | length | 이 조각 데이터의 바이트 길이 |

**오프셋은 안 적는다.** 조각은 목차 바로 뒤에서 시작해 빈틈없이 이어지므로, n 번째 조각의 자리는 「목차 끝 + 앞 조각들의 길이 합」이다. 디코더가 목차를 훑으며 만든다.

```
off(0) = 32 + group_count * 4
off(n) = off(n-1) + length(n-1)
```

2026-08-23 까지는 오프셋도 함께 적었다. 조각당 4바이트를 그냥 버리고 있었고, 빼니 세 지표가 함께 올랐다(-0.24 / -0.30 / -0.18%).

목차가 있어야 디코더가 조각을 **아무 순서로나 병렬로** 풀 수 있다. 길이만 적어도 그 성질은 그대로다 — 자리를 미리 다 계산할 수 있기 때문이다. 인코더가 조각을 어떤 순서로 쓰든 결과는 같다.

### 조각 데이터

각 조각은 자기 안에서 닫혀 있다. 다른 조각의 화소·상태를 참조하지 않는다.

**조각의 실제 크기**는 이미지 경계에서 잘린다. 조각의 왼쪽 위가 (ox, oy)일 때

```
gw = min(group_size, width  - ox)
gh = min(group_size, height - oy)
blocks_x = ceil(gw / 8)
blocks_y = ceil(gh / 8)
```

즉 오른쪽 끝과 아래 끝의 조각은 블록 수가 적다. 이렇게 하지 않으면 조각을 크게 잡을수록 이미지 바깥을 통째로 인코딩하게 된다(2026-08-21 실측: 1200×840 이미지에서 조각 1024로 잡으면 조각 64 대비 파일이 76% 커졌다).

목차의 n 번째 항목은 `gx = n % 가로조각수`, `gy = n / 가로조각수` 자리의 조각이다. **가로 우선 래스터** 순서이며, 세로 우선으로 읽으면 오류 없이 뒤섞인 그림이 나온다.

조각 안은 **평면 순서**로 담는다. Y 블록을 전부 담고, 그다음 Cb, 그다음 Cr이다. 평면마다 블록 수가 다를 수 있어서 이 순서가 필요하다. 각 평면 안에서는 **16×16 묶음**을 왼쪽 위부터 가로로 훑는다. 묶음은 **16 → 8 → 4** 로 나뉠 수 있고, 나뉠 수 있는 크기마다 **분할 비트 1개**를 먼저 쓴다. 4×4 는 더 나뉘지 않으므로 분할 비트가 없다.

| 분할 비트 | 뜻 |
|---|---|
| 0 | 그 크기 블록 하나로 담는다 |
| 1 | 절반 크기 네 개로 나눈다. 왼쪽 위 → 오른쪽 위 → 왼쪽 아래 → 오른쪽 아래 순서 |

16→8 의 분할 비트와 8→4 의 분할 비트는 **서로 다른 확률 모델**을 쓴다. 두 자리에서 나눌 확률이 크게 다르기 때문이다.

**평탄한 영역은 큰 블록 하나가 싸고, 무늬가 많은 영역은 작은 블록 넷이 싸다.** 인코더가 두 갈래를 모두 해 보고 실제 비트 값과 왜곡을 견주어 고른다. 이 판단은 크기마다 되풀이되므로 한 묶음 안에 16×16 하나와 4×4 넷이 함께 있을 수 있다.

묶음이 평면 경계를 넘어가면 그래도 16×16으로 다룬다 — 바깥은 가장자리 화소를 복제해 채우고, 복원한 뒤 잘라낸다.

**색차 서브샘플이 켜지면**(flags 비트 1) 색차 평면은 가로세로가 절반이다. 조각 안 색차 크기는 `ceil(gw/2) × ceil(gh/2)`이다.

- 줄이기: 원본 2×2 화소의 평균을 반올림해서 쓴다. 가장자리에서 짝이 없으면 있는 것만 평균한다.
- 되돌리기: 한 화소를 2×2로 그대로 늘린다(최근접).

색차를 줄이면 색 경계가 뭉개진다. 화면 캡처·문자·그래픽에는 켜지 말아야 한다. 그래서 기본값이 아니라 옵션이다.

**인트라 예측**

블록을 그대로 변환하지 않는다. 먼저 **이미 복원된 이웃 화소로 예측을 만들고, 원본에서 그 예측을 뺀 나머지(잔차)를 변환한다.**

디코더가 쓸 수 있는 이웃만 쓴다. 블록 왼쪽 열 **n개**와 위쪽 행 **n개**이며, n 은 블록 한 변이다(4·8·16).

- **조각 경계를 넘지 않는다.** 조각의 위·왼쪽 바깥은 없는 것으로 친다.
- **평면 오른쪽·아래 경계를 넘어가는 자리는 마지막 유효 화소를 복제한다.** 블록이 평면 끝에 걸칠 때 이웃 n 개를 다 채우기 위해서다. 인코더와 디코더가 같은 규칙을 써야 하므로 이것도 규격의 일부다.

모드는 넷이고 블록마다 두 번의 이진 결정으로 적는다. 고정 길이가 아니라 확률 모델이 붙으며, 담는 법은 아래 「부호화」 절에 있다.

| 모드 | 값 | 예측 |
|---|---|---|
| 0 | DC | 쓸 수 있는 이웃 전부의 평균. **반올림이다** — 합에 개수의 절반을 더한 뒤 나눈다(`(sum + cnt/2) / cnt`). 이웃이 하나도 없으면 128 |
| 1 | 세로 | 위쪽 행을 아래로 복사. 위가 없으면 DC 로 떨어진다 |
| 2 | 가로 | 왼쪽 열을 오른쪽으로 복사. 왼쪽이 없으면 DC 로 떨어진다 |
| 3 | 평면 | 왼쪽과 위를 합쳐 기울기를 만든다. 둘 다 있어야 쓴다. 아니면 DC 로 떨어진다 |

**떨어진다**는 말은 디코더도 같은 조건으로 같은 판단을 한다는 뜻이다. 인코더가 쓸 수 없는 모드를 골라도 결과가 어긋나지 않는다.

**떨어지는 모드에도 비트를 낸다.** 조각의 맨 위 줄에서는 세로·평면이 DC 와 같은 그림을 내므로 넷 중 둘만 서로 다른데, 그래도 두 번의 이진 결정을 적는다. 낭비처럼 보이지만 실제로는 아니다 — 같은 그림을 내는 모드가 여럿이면 인코더가 **그중 확률 모델에서 가장 싼 것을 고를 수 있고**, 그 자유가 목록을 줄여 얻는 만큼을 이미 벌고 있다. 실제로 줄여서 재 보니 차이가 없었다(2026-08-23, 아래 이력 참조).

평면 모드의 계산:
```
shift = log2(n)
a = (위쪽 행 n개의 합 + n/2) >> shift
b = (왼쪽 열 n개의 합 + n/2) >> shift
c = 왼쪽 위 대각 화소 (없으면 (a + b + 1) >> 1)

gx = 위쪽 행의 뒤쪽 n/2 개 합 - 앞쪽 n/2 개 합      (나누지 않은 날값이다)
gy = 왼쪽 열의 아래쪽 n/2 개 합 - 위쪽 n/2 개 합

dx = 2x - (n-1),  dy = 2y - (n-1),  denom = n * n
pred(x, y) = clamp0_255( a + b - c + (dx*gx + dy*gy + denom/2) / denom )
```

`gx`·`gy` 를 **나누지 않는다**는 점과 `dx`·`dy` 를 **두 배로 잡는다**는 점이 함께 가야 눈금이 맞는다. 둘 중 하나만 따르면 기울기가 정확히 절반이나 두 배가 된다. 마지막 나눗셈은 C 의 0 쪽 자름이다(음수에서 내림이 아니다).

**인코더는 반드시 복원한다.** 잔차를 양자화한 뒤 되돌려서 실제 복원 화소를 만들고, 그것을 다음 블록의 이웃으로 쓴다. 그러지 않으면 디코더와 어긋난다.

**DC 계수 예측**

인트라 예측을 넣으면 잔차의 평균이 이미 0에 가까워 앞 블록과의 차이를 또 뺄 이유가 없다. 그래서 **DC 계수 예측은 쓰지 않는다.** (2026-08-21 실측으로 확인한 뒤 이 문장을 확정한다.)

조각을 넘어가면 예측이 끊긴다. 그것이 조각 독립을 지키는 대가이고, 조각을 잘게 나눌수록 이 대가가 커진다. 그 크기는 재서 문서에 적는다.

**블록 하나를 담는 법**

1. 모드를 두 번의 이진 결정으로 쓴다. 상위 비트를 먼저 담는다. 두 비트 모두 모델이 붙고, 둘째 비트의 모델은 첫째 비트가 무엇이었는지에 따라 갈린다.

   **모델은 앞 블록의 모드로 한 번 더 갈린다.** 이웃한 블록은 대개 같은 방향으로 예측되기 때문이다. 앞 블록의 모드를 pm(0~3)이라 하면 쓰는 자리는 `690 + pm*3` 부터 셋이다. pm 은 조각마다·평면마다 DC(0)로 되돌아가고, 16→8→4 재귀 순서를 그대로 따라 흐른다. 이어서 잔차의 계수를 지그재그 순서로 늘어놓는다. 4×4면 16개, 8×8이면 64개, 16×16이면 256개다.

지그재그는 왼쪽 위에서 대각선을 번갈아 훑는 순서다. 대각선 번호 `d = x + y`가 작은 것부터 가고, `d`가 짝수면 아래에서 위로, 홀수면 위에서 아래로 훑는다. 이 규칙은 세 크기(4·8·16) 모두에 같게 적용된다.
2. 마지막으로 0이 아닌 계수의 위치 + 1을 구한다. 이것을 `last`라 하고 0 부터 블록 넓이(4×4 면 16, 8×8 이면 64, 16×16 이면 256)까지의 값이다. 전부 0이면 0이다.
3. `last`를 **산술 부호화**로 쓴다(무리는 아래 표의 last 쪽). `last` 는 0 부터 n×n 까지이며, 4×4 면 16, 8×8 이면 64, 16×16 이면 256 이다. 낮은 비트레이트에서는 대부분의 블록이 0이나 1이라 한두 비트로 끝난다. 고정 7비트로 두면 블록마다 최소 9비트가 들어 비트레이트에 하한이 생긴다.
4. 지그재그 순서로 `last`개의 값을 **산술 부호화**로 쓴다. 값은 크기와 부호로 나눠 담는다.

꼬리의 0은 `last`가 잘라내므로 안 쓴다. 중간의 0은 그 무리의 확률 모델이 0 쪽으로 기울어 한 비트보다 적어진다.

**0의 연속을 개수로 묶지 않는 이유** (2026-08-21 실측): JPEG처럼 (건너뛴 개수, 값) 쌍으로 담아 봤더니 **BD-rate가 PSNR 기준 +6.19%, SSIM 기준 +6.63%로 나빠졌다.** 저주파는 계수가 대부분 0이 아니라서 '건너뛴 개수 = 0'이 자주 나오고, 그것을 쓰는 데 비영 계수마다 1비트가 더 든다. 그 비용이 고주파에서 0을 묶어 버는 것보다 컸다. 자리에 따라 두 방식을 갈라 쓰는 안은 단순성 예산 때문에 보류한다.

**이진 산술 부호화 (레인지 코더)**

골롬-라이스는 값이 기하 분포라고 가정한다. 실제 계수는 그렇지 않고, 특히 "0인가 아닌가"의 확률이 자리마다 크게 다르다. 산술 부호화는 그 확률을 그대로 쓰므로 **한 비트보다 적은 양으로도 담을 수 있다.**

**확률 모델**은 11비트 정수다. 시작값은 반반(1024)이 아니라 아래 「시작 확률표」에서 오며, 비트를 쓰거나 읽을 때마다 32분의 1씩 당긴다.

```
0을 담았으면  p += (2048 - p) >> 5
1을 담았으면  p -= p >> 5
```

**레인지 코더**는 `low`(64비트)와 `range`(32비트)를 들고 간다. 비트마다 `bound = (range >> 11) * p`를 계산해 범위를 나눈다. `range`가 2^24 아래로 내려가면 상위 바이트를 내보내고 8비트 민다. 미는 것은 **32비트 안에서** 해야 한다 — 64비트로 밀면 이미 내보낸 바이트가 남아 다음 번에 가짜 자리올림으로 읽힌다.

인코더는 시작할 때 캐시가 하나 차 있다고 보지만 **첫 밀어내기에서 나오는 0 한 바이트는 안 낸다.** 그때는 캐시가 비어 있고 자리올림도 없어 건너뛰어도 뒤가 안 어긋난다. 디코더는 **네 바이트**를 읽어 시작한다.

2026-08-23 까지는 그 0 을 파일에 넣고 디코더가 다섯 바이트를 읽어 버렸다. 조각마다 1 바이트를 그냥 버리는 셈이었고, 조각이 많은 저품질에서 파일의 0.46% 였다.

**시작 확률은 학습된 표에서 온다.** 조각마다 확률이 반반에서 출발하면 조각이 끝날 때쯤에야 자리를 잡고 다음 조각에서 다시 반반으로 돌아간다. 조각을 잘게 나눌수록 이 낭비가 커진다. 그래서 시험 이미지 여덟 장을 품질 네 단계로 담아 보고, 조각이 끝난 시점의 확률을 모아 평균한 값을 시작값으로 깐다. 표는 702 칸이고 `src/rangecoder.c` 의 `ingot_prob_init` 에 있다.

**이 표는 규격의 일부다.** 디코더도 같은 값에서 출발해야 한다. 그리고 어디선가 뚝 떨어진 숫자가 아니라 `tools/learn_probs.py` 가 만든 것이다. 같은 이미지·같은 품질로 두 바퀴 돌리면 같은 표가 나온다.

```console
$ python tools/learn_probs.py
--- 바퀴 1/2 ---   (반반에서 출발해 모은다)
--- 바퀴 2/2 ---   (첫 바퀴의 표를 깔고 다시 모은다)
src/rangecoder.c 에 심었습니다.
```

바퀴를 둘로 고정한 이유는 재현성이다. 첫 바퀴는 "반반에서 시작하면 어디로 가는가"를 담고, 둘째 바퀴는 "좋은 값에서 시작하면 어디로 가는가"를 담아 실제 쓰임에 가깝다. 바퀴 수를 안 정해 두면 같은 절차가 같은 표를 못 낸다.

**모델을 붙이는 자리** — 모두 702개다.

| 자리 | 번호 | 무엇 |
|---|---|---|
| 계수 무리 86개 × 모델 8개 | 0 ~ 687 | 크기 깃발 셋과 지수 골롬 접두 셋(나머지 둘은 예비) |
| 분할 비트 | 688 ~ 689 | 16→8 과 8→4 |
| 예측 모드 | 690 ~ 701 | 앞 블록의 모드 넷 × 이진 트리 셋 |

**무리 번호를 매기는 산식.** 계수의 지그재그 자리를 k, 블록 한 변을 n, 색차 여부를 plane 이라 하면

```
kk   = (n == 8) ? k : (k * 64) / (n * n)          자리를 64 눈금으로 다시 잰다
band = kk == 0 ? 0 : kk <= 5 ? 1 : kk <= 20 ? 2 : 3
sz   = (n == 16) ? 1 : 0                          4×4 와 8×8 은 한 무리다
lvl  = 왼쪽 (u-1,v)·위 (u,v-1)·왼쪽위 (u-1,v-1) 에 이미 담긴 값의 절대값 합을
       0 / 1 / 2~3 / 4~7 / 8 이상 다섯 단계로 나눈 것
무리 = (sz * 4 + band) * 5 + lvl,  색차면 + 40

last 무리 = 80 + sz2 * 2 + (색차면 1),  sz2 는 n=4 → 0, n=8 → 1, n=16 → 2
```

이웃 셋은 모두 지그재그 순서상 반드시 앞이므로 디코더도 같은 값을 안다. 무리마다 쓰는 모델 여덟 자리는 `무리 × 8` 에서 시작한다.

**값 담는 법**: 크기 `k`를 깃발 셋으로 먼저 묻는다 — `k > 0`인가, `k > 1`인가, `k > 2`인가. 셋 다 모델이 붙어 있다. 대부분의 계수가 0이나 ±1이라 여기서 끝난다.

**단, 지그재그 순번이 `last - 1`인 계수는 첫 깃발을 적지 않는다.** `last`가 「마지막으로 0이 아닌 계수의 자리 + 1」이므로 그 자리의 계수는 정의상 0이 아니고, 디코더도 `last`를 먼저 읽어 그 사실을 안다. 그 자리에서는 `k > 1`인가부터 시작하며, 나머지는 완전히 같다.

깃발을 건너뛰어도 모델을 새로 두지 않는 이유는 뜻이 그대로이기 때문이다. 둘째 깃발은 두 경로 모두 「`k >= 1`인 상태에서 `k > 1`인가」이므로 조건이 같고, 따라서 같은 모델 자리를 나눠 써도 분포가 섞이지 않는다.

이것을 빠뜨리면 답이 정해진 질문에 비트를 낸다. **다시 셀 수 있다** — `python tools/certain_flag.py` 가 표준 사진 8장을 품질 셋으로 담아 그 자리를 센다. 2026-08-23 실측은 품질 6·20·40 에서 각각 11,277 · 5,965 · 3,953 자리였고 **셋 다 100.0000% 가 「0 아님」**이었다. 비율이 100 이 아니면 규격이나 인코더에 결함이 있다는 뜻이다.

`k >= 3`이면 `k - 3 + 1`을 지수 골롬으로 담는다. **접두부(자릿수를 세는 1들과 끝의 0)에도 모델을 붙인다.** 접두부는 "얼마나 큰가"를 말하는 자리라 분포가 한쪽으로 쏠려 있어 모델이 값을 한다. 접미부는 거의 균등하므로 **모델을 아예 안 쓰고** 범위를 절반씩 직접 자른다(`range >>= 1`). p=1024 모델로 구현하면 범위가 달라져 계수 3 이상이 한 번 나오는 순간 어긋난다. 접두부 모델은 자리 깊이별로 셋이고, 셋째 자리부터는 같은 모델을 쓴다.

부호도 같은 방식으로 한 비트다. **1 이 음수다.**

**깃발을 셋으로 정한 근거** (2026-08-22 실측): 깃발 없이 지수 골롬으로 넘기던 판에서는 전체 비트의 38.8%가 모델 없이 반반으로 나가고 있었다. 확률이 반반인 자리는 한 비트도 못 아낀다. 깃발 3·5·8을 재 보니 3이 가장 좋았고, 8은 오히려 나빠진다 — 깃발이 많아지면 뒤쪽 깃발이 표본을 못 받아 모델이 안 여문다.

산술 부호화는 1970년대 후반에 공개됐고 레인지 코더도 오래된 구조라 특허 걱정이 없다(관찰 — 법률 검토는 아직 아니다).

**조각의 끝**: 레인지 코더가 남은 상태를 다섯 바이트로 흘려 내며 끝맺는다(0비트 채우기가 아니다). 조각마다 코더 상태와 **확률 모델 702 칸 전부**가 「시작 확률표」의 값으로 되돌아간다. 조각 사이에 아무 정보도 흐르지 않는다.

가장자리에서 조각이나 블록이 이미지 밖으로 나가면, 마지막 화소를 복제해 채운다(edge replication). 채운 부분은 디코딩 후 잘라낸다.

## 색변환 (정수, 되돌릴 수 있어야 한다)

BT.601 계수의 고정소수점 근사다. 16비트 정밀도로 곱하고 반올림한다.

```
Y  = ( 19595*R + 38470*G +  7471*B + 32768) >> 16
Cb = (-11056*R - 21712*G + 32768*B + 32768) >> 16 + 128
Cr = ( 32768*R - 27440*G -  5328*B + 32768) >> 16 + 128
```

역변환:
```
R = Y + ((91881*(Cr-128) + 32768) >> 16)
G = Y - ((22554*(Cb-128) + 46802*(Cr-128) + 32768) >> 16)
B = Y + ((116130*(Cb-128) + 32768) >> 16)
```

모든 결과는 0~255로 자른다. **이 변환은 되돌릴 수 없다** — 색변환 자체에서 손실이 난다. v0는 그것을 인정하고, 대신 **인코더와 디코더가 정확히 같은 정수 결과를 낸다**는 것만 보장한다.

## 변환 (정수 4×4 · 8×8 · 16×16)

v0는 정수 계수 행렬을 쓴다. 부동소수점 삼각함수를 쓰지 않는다.

행렬은 DCT-II를 256배 해서 반올림한 정수다. 이 표가 규격의 일부다.

```
 91,  91,  91,  91,  91,  91,  91,  91
126, 106,  71,  25, -25, -71,-106,-126
118,  49, -49,-118,-118, -49,  49, 118
106, -25,-126, -71,  71, 126,  25,-106
 91, -91, -91,  91,  91, -91, -91,  91
 71,-126,  25, 106,-106, -25, 126, -71
 49,-118, 118, -49, -49, 118,-118,  49
 25, -71, 106,-126, 126,-106,  71, -25
```

4×4 와 16×16 블록에는 같은 방식으로 만든 4점·16점 표를 쓴다.

```
 64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64,  64
 90,  87,  80,  70,  57,  43,  26,   9,  -9, -26, -43, -57, -70, -80, -87, -90
 89,  75,  50,  18, -18, -50, -75, -89, -89, -75, -50, -18,  18,  50,  75,  89
 87,  57,   9, -43, -80, -90, -70, -26,  26,  70,  90,  80,  43,  -9, -57, -87
 84,  35, -35, -84, -84, -35,  35,  84,  84,  35, -35, -84, -84, -35,  35,  84
 80,   9, -70, -87, -26,  57,  90,  43, -43, -90, -57,  26,  87,  70,  -9, -80
 75, -18, -89, -50,  50,  89,  18, -75, -75,  18,  89,  50, -50, -89, -18,  75
 70, -43, -87,   9,  90,  26, -80, -57,  57,  80, -26, -90,  -9,  87,  43, -70
 64, -64, -64,  64,  64, -64, -64,  64,  64, -64, -64,  64,  64, -64, -64,  64
 57, -80, -26,  90,  -9, -87,  43,  70, -70, -43,  87,   9, -90,  26,  80, -57
 50, -89,  18,  75, -75, -18,  89, -50, -50,  89, -18, -75,  75,  18, -89,  50
 43, -90,  57,  26, -87,  70,   9, -80,  80,  -9, -70,  87, -26, -57,  90, -43
 35, -84,  84, -35, -35,  84, -84,  35,  35, -84,  84, -35, -35,  84, -84,  35
 26, -70,  90, -80,  43,   9, -57,  87, -87,  57,  -9, -43,  80, -90,  70, -26
 18, -50,  75, -89,  89, -75,  50, -18, -18,  50, -75,  89, -89,  75, -50,  18
  9, -26,  43, -57,  70, -80,  87, -90,  90, -87,  80, -70,  57, -43,  26,  -9
```

**순변환**은 `F = M · X · Mᵀ` 다. 행 방향으로 먼저 곱해 32비트에 모으고, 이어서 열 방향으로 곱해 모은 뒤 시프트한다. 첨자로 적으면

```
tmp[y][u] = Σ_x  M[u][x] * X[y][x]
F[v][u]   = (Σ_y  M[v][y] * tmp[y][u] + (1 << (shift-1))) >> shift
```

시프트는 **세 크기 모두 14비트**다. 2026-08-23 까지 16×16 만 15비트였다.

**역변환**은 `X = Mᵀ · F · M` 이다. **순변환과 방향이 반대**이고, 표가 대칭이 아니므로 같은 방향으로 읽으면 전혀 다른 그림이 나온다(실측: 방향만 바꾸면 34.59 dB 가 9.94 dB 가 되고 화소 99.6% 가 달라진다).

```
tmp[y][u] = Σ_v  M[v][y] * F[v][u]
X[y][x]   = (Σ_u  M[u][x] * tmp[y][u] + (1 << (shift-1))) >> shift
```

시프트는 **세 크기 모두 18비트**다. 2026-08-23 까지 16×16 만 17비트였다. 역변환의 누산은 **64비트**로 한다 — 계수가 규격 상한(±32767)까지 찬 16×16 이면 둘째 단계가 32비트 한계의 31.6 배까지 간다.

**시프트의 반올림 방향이 규격이다.** `(s + (1 << (shift-1))) >> shift` 는 C 에서 산술 오른쪽 시프트이므로 **음수에서도 내림**이다(−∞ 쪽). 0 쪽으로 자르면 안 된다 — 계수의 절반이 음수라 언제나 걸리고, 실측으로 서브픽셀 91.5% 가 달라지며 34.59 dB 가 28.25 dB 가 된다. 색공간 변환의 시프트도 같은 규칙이다. **세 크기가 같은 눈금인 것은 뜻이 있다** — 같은 양자화 스텝이 세 크기에서 같은 세기로 들어야 인코더의 크기 선택이 공정해진다.

2026-08-23 까지 16×16 만 어긋나 있었다. 상수 잔차 100 을 넣으면 DC 가 4×4 는 1600, 8×8 은 3235, 16×16 은 3200 이었고, 직교 기준(400/800/1600)으로 나누면 배수가 4.00 / 4.04 / **2.00** 이었다. 즉 같은 스텝이 16×16 에서만 두 배 거칠게 들었고, 인코더가 「16 을 넷으로 나눌까」를 견줄 때 두 후보가 서로 다른 세기의 양자화를 받아 그 판단이 사실상 죽어 있었다.

**되돌림 오차**: 양자화 없이 순변환 후 역변환만 하면 화소당 최대·제곱평균제곱근이 4×4 는 (2, 0.70), 8×8 은 (5, 1.30), 16×16 은 (5, 1.27) 이다(무작위 블록 실측, 2026-08-22 재측정). 2026-08-21 까지 여기 8×8 값으로 적혀 있던 (2, 0.70)은 실제로 4×4 의 값이었다 — 그 잣대로 재면 옳게 만든 디코더가 불합격한다.

이 값을 재는 시험은 아직 없다. 골든 파일은 바이트와 복원 화소를 잠글 뿐 변환만 따로 재지 않는다.

계수 범위는 이 배율에서 16비트 부호 있는 정수에 들어간다.

## 양자화

균일 스칼라 양자화다.

```
base = 1 + (quality * 2 + (quality * quality) / 16) * 32 / 16   (quality 0~63 → base 1~749)
```

**스텝은 계수 자리마다 다르다.** 지그재그 순서가 아니라 블록 안 좌표로 정한다. 계수의 자리를 (u, v)라 하면 — u는 가로 주파수, v는 세로 주파수다 —

```
step = (base * (16 + 1 * (u + v))) >> 4
색차 평면이면  step = (step * 20) >> 4
step 이 0 이면 1 로 올린다
```

높은 자리일수록 거칠게 양자화한다. 사람 눈이 고주파 오차를 덜 보기 때문이고, 이 값 1은 지각 지표로 재서 정했다(2026-08-22, 아래 이력 참조). 색차의 20/16 도 재서 정한 값이다.

**되돌리는 법**은 곱하기 하나다.

```
coef' = level * step
```

**반올림 자리는 인코더의 자유다.** 규격은 `level`을 그대로 받아 되돌릴 뿐이므로, 인코더가 반올림을 어디서 하든 파일은 읽힌다. 우리 인코더는 절반이 아니라 **6/16 자리**에서 반올림한다(2026-08-22 기준. 5/16 였다가 인코더의 자 두 곳을 고친 뒤 다시 재서 6 으로 올렸다 — 아래 이력 참조).

**이 절은 2026-08-22 에 고쳤다.** 그 전까지 여기에는 `coef' = level * step` 만 적혀 있고 "계수 위치별 가중치는 v0 에 없다"고 쓰여 있었는데, 디코더는 그때도 자리 가중과 색차 보정을 쓰고 있었다. q20 에서 규격서대로면 스텝이 66 하나인데 실제로는 66 에서 236 까지다. 규격서만 보고 만든 디코더는 우리 파일을 한 장도 못 읽었을 것이다.

## 손상 감지 해시

머리말 20번 자리(4바이트, 빅엔디언)는 **목차와 조각 데이터 전체**의 FNV-1a 32비트 해시다. 머리말 32바이트를 뺀 나머지 전부가 대상이다.

```
h = 2166136261
바이트마다:  h = (h XOR 바이트) * 16777619    (32비트 안에서 자름)
h 가 0 이면 1 로 바꾼다
```

**0 은 「해시 없음」이다.** 그래서 인코더가 마지막에 0 을 낼 수 없게 1 로 올린다. 디코더는 이 자리가 0 이면 검사를 건너뛰고, 0 이 아니면 다시 계산해 다르면 거절한다.

**왜 필요한가.** 산술 부호화는 어떤 비트열이든 그럴듯한 값으로 읽어 낸다. 이것이 없으면 망가진 파일이 오류가 아니라 **조용히 다른 그림**이 된다. 실측으로 손상 300건 중 44건이 그림을 냈고, 40건이 20 dB 아래, 최악이 11.55 dB 였다. 이 자리를 쓰면서 그 44건이 0건이 됐고 파일 크기는 한 바이트도 안 늘었다.

## 블록 경계 필터

flags 비트 2 가 1 이면 디코더는 조각의 한 평면을 다 푼 **직후** 그 조각 사각형 안에서 블록 경계를 편다. 인코더는 이 필터를 돌리지 않는다.

**예측은 필터 전 화소를 본다.** 그래서 인코더의 복원 버퍼를 안 걸러도 디코더와 어긋나지 않는다. 양쪽을 보는 필터는 원리상 다음 블록의 예측 이웃이 될 수 없다 — 블록 B 의 왼쪽 경계를 펴려면 B 자신의 화소가 있어야 하는데, B 를 얻으려면 예측이 먼저 끝나야 하기 때문이다. HEVC·AV1 의 인트라 예측이 언제나 필터 전 화소를 쓰는 이유가 같다.

**조각 독립.** 필터는 조각 원점의 경계를 건드리지 않는다. 블록을 복원할 때마다 그 블록의 왼쪽·위 경계를 4×4 칸 지도에 찍되, 조각 원점에 붙은 변은 안 찍는다. 그래서 조각을 어떤 순서로 풀어도 결과가 같다.

**어디를 펴는가.** 지도에 찍힌 경계 중 8 의 배수 자리만 편다. 세로 경계는 네 줄씩, 가로 경계는 네 칸씩 훑는다.

조각 안에서 도는 범위는 이렇다. 조각 크기를 `gw × gh` 라 하면

```
세로 경계:  y = 0, 4, 8, ...  (y + 4 <= gh 인 동안)
            x = 8, 16, ...    (x + 3 <= gw - 1 인 동안)
가로 경계:  y = 8, 16, ...    (y + 3 <= gh - 1 인 동안)
            x = 0, 4, 8, ...  (x + 4 <= gw 인 동안)
```

`x` 와 `y` 가 8 부터 시작하므로 조각의 왼쪽 변과 위 변은 영영 안 편다. 오른쪽·아래 끝에는 한 칸의 여백이 남는데, 필터가 실제로 읽는 가장 먼 화소는 `q2`(경계에서 두 칸)라서 안전에 필요한 것보다 한 칸 더 보수적이다. 그래도 규격이므로 이대로 따른다 — 한 칸을 더 펴면 파일이 달라진다.

**세기.** 자리마다 다른 `step` 이 아니라 **품질만으로 정해지는 기본 단계** `base`(「양자화」 절의 `qstep(quality)`)에서 만든다. 자리 가중과 색차 배수가 붙기 전 값이다.

```
s    = 휘도면 base,  색차면 (base * 20) >> 4      색차 배수는 여기서 한 번만 곱한다
beta = (s * 16) >> 5      64 를 넘으면 64 로 자른다
tc   = (s * 8)  >> 7      24 를 넘으면 24 로 자른다
tc < 1 이거나 beta < 1 이면 그 평면은 아예 안 편다
```

고품질에서는 `tc` 가 0 이 되어 필터가 통째로 꺼진다. 펼 것이 없기 때문이다.

**휘도 경계 하나를 펴는 법.** 경계를 가로지르는 여섯 화소를 `p2 p1 p0 | q0 q1 q2` 라 하면

```
첫째 줄과 넷째 줄에서 2차 차분을 잰다
  dp = |p2 - 2*p1 + p0|,  dq = |q2 - 2*q1 + q0|
네 값의 합이 beta 이상이면 이 경계는 안 편다 (양쪽이 이미 무늬다)

d = (9*(q0 - p0) - 3*(q1 - p1) + 8) >> 4
|d| 가 tc*10 이상이면 이 줄은 안 편다 (진짜 모서리다)
d 를 [-tc, tc] 로 자른 뒤
  p0' = p0 + d,  q0' = q0 - d
2차 차분의 합이 (beta + (beta >> 1)) >> 3 미만인 쪽만 한 겹 더 편다
  p1 += clip(-(tc>>1), tc>>1, ((((p2 + p0 + 1) >> 1) - p1 + d) >> 1))
  q1 += clip(-(tc>>1), tc>>1, ((((q2 + q0 + 1) >> 1) - q1 - d) >> 1))
```

**두 가지를 못박는다.** 첫째, `p1`·`q1` 식에 들어가는 `p0`·`q0` 는 **고치기 전의 원래 값**이다. `p0'`·`q0'` 를 쓰면 다른 그림이 나온다. 둘째, 모든 나눗셈은 **산술 오른쪽 시프트(내림)** 다. 괄호 안이 음수가 되는 일이 흔한데, C 의 `/2`(0 쪽 자름)와 `>>1`(내림)은 그때 1 만큼 갈린다.

**색차는 한 겹만 편다.** `d = clip(-tc, tc, (((q0 - p0) << 2) + p1 - q1 + 4) >> 3)` 로 `p0`·`q0` 만 고친다. 평탄 판정도 모서리 판정도 안 한다 — 색차는 언제나 편다.

모든 결과는 0~255 로 자른다. 평탄 판정과 모서리 판정이 요점이다. 이것이 없이 모든 경계를 같은 힘으로 밀면 무늬까지 뭉개진다 (2026-08-21 에 그렇게 만들어 세 세기 모두 졌다).


## 디코더가 반드시 거절해야 하는 것

1. 시그니처 불일치
2. version이 0이 아님
3. flags의 예약 비트가 하나라도 1
4. hash32가 목차·조각 데이터에서 다시 계산한 값과 다름
5. width나 height가 0이거나 65535를 넘음
6. group_log2가 6 미만이거나 10 초과
7. group_count가 계산값과 다름
8. 목차의 offset·length가 파일 밖을 가리킴
9. 조각 길이가 그 조각에 필요한 계수 수와 안 맞음
10. 전체 화소 수가 상한(1억)을 넘음

거절은 오류 코드로 돌아간다. 프로세스를 죽이지 않는다.

## 아직 없는 것 (일부러)

타일 병렬 실행, 다중 비트 깊이, 알파, 색 관리, 컨테이너, 애니메이션, 무손실 모드.

예측·가변 블록·색차 서브샘플·블록 경계 필터는 이 목록에 있었으나 넷 다 들어갔다(각각 「예측」·「블록 나누기」·「색차」·「블록 경계 필터」 절). 필터는 예측 뒤에 걸리므로 엄밀한 뜻의 인루프는 아니지만, 디코더가 반드시 돌려야 하는 규범이다.

## 바꿀 때의 규칙

이 문서를 먼저 고치고 그다음 코드를 고친다. 골든 파일이 깨지면 **그것은 문법이 바뀌었다는 뜻**이다.

**공개 전 예외**: 아직 아무도 이 포맷을 쓰지 않으므로, 첫 공개 전까지는 version을 0으로 고정하고 골든 파일만 갱신한다. 갱신할 때는 무엇이 왜 바뀌었는지를 아래 이력에 적는다. 첫 공개 시점에 version을 1로 올리고, 그 뒤로는 문법이 바뀔 때마다 올린다.

## 문법 변경 이력 (공개 전)

| 날짜 | 바뀐 것 | 잰 이득 |
|---|---|---|
| 2026-08-21 | 첫 판 — 정수 DCT, 균일 양자화, 지수 골롬 | 기준선 |
| 2026-08-21 | 조각을 이미지 경계에서 자름 | 조각 크기에 따른 낭비 제거 (조각 1024에서 76% 개선) |
| 2026-08-21 | DC 예측 (조각·평면 안에서 앞 블록과의 차이) | BD-rate -9.44% (이미지 8장 평균, 3차 보간 기준) |
| 2026-08-21 | 계수 위치·평면별 양자화 가중 | 고주파 기울기는 이득 0이라 껐다. 색차 배수 20만 채택 (PSNR -0.58%, SSIM -1.51%) |
| 2026-08-21 | 적응형 골롬-라이스 부호화 (무리 8개) | 여기까지 누적 **PSNR -11.72%, SSIM -10.42%** |
| 2026-08-21 | (되돌림) 0의 연속을 개수로 묶기 | **+6.19% / +6.63% 손해**라 되돌렸다 |
| 2026-08-21 | 조각 안을 평면 순서로 재배치 | 실제 사진에서는 -0.06%. 색차 줄이기를 넣기 위한 구조 변경이다 |
| 2026-08-21 | 색차 줄이기(4:2:0) 옵션 | 표준 사진에서 **PSNR -10.16%, SSIM -13.18%**. 화면 캡처에서는 -8.5 dB 손상이라 기본값은 4:4:4 |
| 2026-08-21 | 인트라 예측 4모드 + 인코더 복원 루프 | **PSNR -34.44%, SSIM -21.01%**. 지금까지 가장 큰 한 방이다 |
| 2026-08-21 | 양자화 범위를 1~127 에서 1~375 로 | 낮은 비트레이트에 닿게 했다. 그 전에는 곡선이 안 겹쳐 BD-rate 를 절반도 못 쟀다 |
| 2026-08-21 | 블록 머리말(last)을 고정 7비트에서 적응 부호화로 | 같은 화질에서 파일이 q40 에서 49%, q63 에서 58% 줄었다. 블록마다 최소 9비트가 만들던 하한을 없앴다 |
| 2026-08-21 | (버그 수정) 이웃 화소가 평면 오른쪽·아래 경계를 넘어가던 것 | 손상 입력 시험이 잡았다. 정상 입력에서도 가장자리 블록의 예측이 틀렸다 |
| 2026-08-21 | 가변 블록 (16×16 하나 또는 8×8 넷) + 율왜곡 기준 선택 | **JPEG을 넘어섰다** — 4:4:4에서 PSNR BD-rate −1.0% |
| 2026-08-21 | 모드 선택을 잔차 절대합에서 실제 비트·왜곡 기준으로 | 위 변경에 포함해 쟀다 |
| 2026-08-21 | (되돌림) 32×32 블록까지 확장 | **PSNR +13.2%로 나빠져 되돌렸다.** 32점 변환의 왕복 오차가 더 크고(최대 7 대 4), 예측 모드가 넷뿐이라 큰 블록에서 안 맞는다(추정). 성숙한 코덱이 32×32를 쓸 때는 예측 모드가 수십 개다 |
| 2026-08-21 | (되돌림) 블록별 적응 양자화 2비트 | **PSNR +15.0%, SSIM +11.8%로 둘 다 나빠져 되돌렸다.** 분산 임계값과 곱셈값을 실측 없이 정한 것이 원인으로 보인다(추정). 지각 지표를 붙인 뒤 제대로 훑어서 다시 시도한다 |
| 2026-08-21 | (되돌림) 예측 모드를 넷에서 여덟로 | PSNR -1.0% → -0.4%. 모드 비트 하나가 늘어난 값이 이득과 상쇄됐다 |
| 2026-08-21 | **골롬-라이스를 이진 산술 부호화로 교체** | **두 지표 모두에서 JPEG을 넘었다**: PSNR -16.7%, SSIM -9.9%. WebP 와도 PSNR 기준 1.2% 차이 |
| 2026-08-21 | 계수 무리에 **직전 계수의 크기**를 더함 (무리 26개) | PSNR -16.7% → **-17.6%**, SSIM -9.9% → **-10.9%**. WebP 와 PSNR 기준 0.2% |
| 2026-08-21 | (되돌림) 이웃 단계를 셋에서 다섯으로 | 무리가 늘자 무리마다 표본이 줄어 확률이 늦게 자리를 잡았다 |
| 2026-08-21 | (되돌림) 예측 모드 여덟, 두 번째 시도 | 산술 부호화로 모드 비트가 싸졌는데도 PSNR -17.6% → -16.7%. 방향 예측이 8×8 이상 블록에서 값을 못 한다 |
| 2026-08-21 | **양자화 반올림 자리를 절반에서 5/16 으로** (데드존) | **WebP 를 넘었다**: PSNR -17.6% → **-25.1%**, SSIM -10.9% → **-19.8%**. WebP 대비 PSNR **-8.1%** |
| 2026-08-21 | (되돌림) 고주파 양자화 가중치 재탐색 | 0 이 최선. 1·2·3 모두 두 지표에서 나빠졌다 |
| 2026-08-21 | (되돌림) 블록 경계 필터 (조각 뒤처리) | 세기 8·16·24 모두 손해. 예측이 필터 전 값을 보므로 인루프 이득이 없고, 데드존으로 이미 아낀 상태에서 필터가 무늬까지 뭉갠다 |
| 2026-08-21 | 인코더에 **비트 값 표** 도입 (확률 → 1/16 비트) | 산술 부호화에서 결정 개수는 비트 수가 아니다. 값을 정확히 재게 됐다. 그 자체 이득은 미미하지만, 앞으로 비용을 저울질하는 모든 판단의 바탕이다 |
| 2026-08-21 | (되돌림) 꼬리 계수 자르기 (인코더 전용) | λ 를 87·50·30·15 로 훑어도 전부 손해. 인트라 예측이라 한 블록을 깎으면 이웃 예측까지 나빠지는데, 블록 하나만 보는 비용 계산은 그 전파를 모른다 |
| 2026-08-21 | (되돌림) 블록마다 반올림 자리 바꾸기 | 평탄한 블록을 곱게 가 봤더니 임계 40·120·300 모두 손해. SSIM 까지 나빠졌다 — 평탄한 곳에 비트를 더 쓰는 것보다, 그 비트를 아껴 전체 품질을 올리는 편이 낫다 |
| 2026-08-21 | **4×4 블록 추가** (16 → 8 → 4 재귀 분할) | WebP 와의 거리가 줄었다: PSNR -8.2% → **-8.5%**, SSIM +4.4% → **+2.7%**. JPEG 대비 PSNR 은 -25.1% → -24.2% 로 조금 물러섰다 — 작은 블록은 블록마다 드는 머리말이 있어 저비트레이트에서 손해다 |
| 2026-08-21 | (되돌림) 4×4 에만 방향 예측 넷 추가 | 작은 블록이면 값을 하리라 봤는데 여기서도 손해였다 (PSNR -24.2% → -23.5%). 예측 모드를 늘리는 갈래는 세 번 재고 세 번 다 졌다 — 모드 비트가 아니라 이웃 화소를 다듬는 쪽에 답이 있을 것이다 |
| 2026-08-21 | **last 무리를 블록 크기별로 나눔** (무리 30개) | **JXL 을 넘었다**: PSNR -24.2% → **-24.6%**, JXL 대비 +8.4% → **-2.1%**. 4×4 의 last 는 대개 한둘인데 16×16 은 수십까지 간다 — 한 무리에 섞어 두면 어느 쪽도 제대로 못 배운다 |
| 2026-08-21 | 인코더: 모드 선별을 두 단계로 (잔차 절대합 → 앞선 둘만 담아 보기) | 인코딩 2.73초 → 1.80초. 품질은 거의 그대로 |
| 2026-08-21 | 인코더: **분할 조기 종료** (통째로 담는 값이 24비트보다 싸면 나누기를 재지 않음) | 속도와 품질이 함께 올랐다: 1.80초 → **1.15초**, PSNR -24.6% → **-25.6%**, JXL 대비 -2.1% → **-3.2%**. 우리 율왜곡 판단이 분할을 과하게 골라 왔다는 뜻이다 |
| 2026-08-21 | 인코더: 조각마다 자리를 미리 잘라 담고 나중에 이어 붙이기 | 결과 바이트는 그대로. 조각 독립이 실제 병렬 인코딩으로 쓰일 수 있게 됐다 (OpenMP 지시문 한 줄). 이 기계에는 OpenMP 런타임이 없어 **병렬 속도는 재지 못했다** |
| 2026-08-21 | (되돌림) 예측 모드에 맞는 변환(ADST) | PSNR -25.6% → -19.3%. 스캔 순서를 두 방향으로 바꿔 봐도 더 나빠졌다. VP9 이 이 조합으로 이득을 보는 것은 예측 모드가 열 가지 넘게 있어 V·H 가 자주 뽑히기 때문이다. 우리는 모드가 넷뿐이라 ADST 를 쓸 자리가 적고, 그 대신 무리 모델과 지그재그가 DCT 분포에 맞춰져 있어 손해만 남는다 |
| 2026-08-21 | 계수 무리를 블록 크기로도 가름 (무리 54개) | PSNR -25.6% → **-25.7%**. 미미하지만 두 지표 모두에서 손해가 없다 |
| 2026-08-21 | (되돌림) 네 번째 예측 모드를 TM·Paeth 로 교체 | 기울기를 이어 붙이는 지금 방식이 가장 좋았다. TM 은 -24.8%, Paeth 는 -24.2% 로 물러섰다 |
| 2026-08-21 | (되돌림) 블록 머리말 무리에 직전 블록의 길이 | 두 지표 모두 자릿수까지 그대로였다. 확률 모델이 이미 적응형이라 그 경향을 스스로 따라가고 있었다 — 무리를 갈라도 새로 알려 줄 것이 없다 |
| 2026-08-21 | (유지) 색차 양자화 가중치 20 | 16·26·32 를 다시 재 봤으나 20 이 SSIM 에서 가장 좋다 |
| 2026-08-21 | **지각 지표(VMAF)를 벤치의 셋째 축으로** | 잰 것이지 바꾼 것은 아니다. 그런데 그림이 달라졌다: PSNR·SSIM 으로 이긴 WebP·JXL 에 VMAF 로는 각각 **+33.9%, +42.4%** 로 진다. JPEG 과는 -3.2% 로 겨우 앞선다. 우리 인코더가 제곱 오차만 보고 판단해 온 대가다 |
| 2026-08-21 | (v1 에서는 보류) 고주파 양자화 가중치 1 | VMAF 가 JPEG 대비 -3.2% → **-15.1%**, WebP 대비 +33.9% → +25.3% 로 크게 좋아진다. 대신 PSNR 이 -25.7% → -23.3% 로 물러선다. 세 지표가 갈리는 자리라 한 지표만 보고 정할 수 없다 — VMAF 는 동영상 평가로 학습된 지표이므로, SSIMULACRA2 로 교차 확인한 뒤 정한다 |
| 2026-08-21 | **지각 지표를 SSIMULACRA2 로 교체** (VMAF 대신) | 파이썬 구현이 있어 쓸 수 있게 됐다. 이미지 전용 지표라 VMAF 보다 판단이 정확하다. 대신 느리다 — 한 장에 이 초 |
| 2026-08-21 | **고주파 양자화 가중치 0 → 1** | 앞의 보류를 뒤집는다. SSIMULACRA2 로 재 보니 **가중치 0 에서는 JPEG 에게도 진다(+5.6%)**. 1 은 세 지표 모두에서 JPEG 을 이기는 가장 작은 값이다: PSNR -23.3%, SSIM -21.1%, SSIMULACRA2 -11.3%. 2 로 더 가면 지각 지표는 나아지지만 PSNR 로 WebP·JXL 을 잃는다 |
| 2026-08-22 | (정정, 문법 무변경) 조각 독립의 대가를 다시 잼 | v0 에서 잰 **1.4%** 를 v1.1 까지 그대로 걸어 두고 있었다. 산술 부호화가 들어간 뒤로는 조각 경계마다 확률표를 처음부터 배우므로 대가의 성격이 바뀌었다. 다시 재니 한 장을 한 조각으로 담은 것 대비 256(기본) **+2.7%**, 128 +9.8%, 64 **+29.7%** 다. 21 배 어긋난 숫자가 공개돼 있었다 |
| 2026-08-22 | (정정) 디코딩 속도 "견줄 만함" 철회 | 프로세스가 시작만 하는 데 ffmpeg 0.059 초, 우리 CLI 0.028 초가 든다. 그 상수를 상대에게만 지운 채 비교하고 있었다. 빼고 보면 우리 0.068 초 대 JPEG 0.006 초로 **다섯 중 꼴찌**다 |
| 2026-08-22 | (정정) 줄 수 | 전체 1,900 → **2,030**, 디코딩 경로 880 → **1,303** |
| 2026-08-22 | **인코더 수리: 머리말 비트의 눈금** (문법 무변경) | 모드 비트와 나눔 비트의 값을 계산할 때 1/16 비트 눈금을 안 곱하는 자리가 다섯 곳이었다. 인코더가 블록 쪼개는 비용을 실제의 1/16 로 보고 있었다. 고치고 λ 를 45 → 10 으로 다시 잡으니 **모든 칸이 좋아졌다**: PSNR JPEG -23.3% → **-23.7%**, WebP -4.4% → **-5.0%**, JXL +0.1% → **-0.6%**(넘었다), AVIF +70.6% → +69.5%. SSIMULACRA2 도 JPEG -11.3% → -11.8%, AVIF +43.5% → +42.6% |
| 2026-08-22 | (되돌림, 두 번째) 꼬리 계수 자르기 | 자를 고친 뒤 다시 쟀다. PSNR 은 -26.7% → -27.4% 로 좋아지는데 **SSIMULACRA2 가 -16.7% → -8.5%** 로 크게 나빠진다. 꼬리 계수가 곧 세부라서, 제곱 오차는 덜 늘고 지각은 크게 상한다. 첫 시도의 판정은 우연히 맞았고 이유가 달랐다 |
| 2026-08-22 | (효과 미미) 왜곡 자를 지각 쪽으로 | 4×4 소구역마다 분산으로 오차를 눌러 봤다. q20 한 장에서 20,227 → 20,181 바이트로 0.2% 뿐이다. 왜곡 자는 모드와 분할만 고르는데 지각 손실의 본체는 양자화 결정에 있다. 양자화를 율왜곡으로 판단하게 만들어야 이 자가 값을 한다 |
| 2026-08-22 | **(정정) 규격서의 역양자화 절** | 규격서에는 `coef' = level * step` 하나만 적고 "계수 위치별 가중치는 v0 에 없다"고 쓰여 있었는데, 디코더는 그때도 자리 가중 `(16 + (u+v))/16` 과 색차 `20/16` 을 쓰고 있었다. q20 에서 규격서대로면 스텝 66 하나인데 실제는 66~236 이다. **규격서만 보고 만든 디코더는 우리 파일을 한 장도 못 읽는다.** 코드는 그대로 두고 문서를 코드에 맞췄다 |
| 2026-08-22 | **인코더 수리: λ 정밀도** (문법 무변경) | λ 를 정수로 들고 다녀 고품질에서 0 으로 뭉개졌다. q1 이면 (3×3×10)/1600 = 0 이다. 그 구간에서 율왜곡 판단이 통째로 죽어 있었다. 왜곡을 256 배 눈금으로 키워 소수 자리를 만들었다. **WebP 를 세 지표 모두에서 잡았다**: PSNR -9.5%, SSIM -3.9%, SSIMULACRA2 -0.0% (6장) |
| 2026-08-22 | **데드존 5 → 6**, 역변환 누산 64비트 | 인코더의 자 두 곳을 고친 뒤 데드존을 다시 훑으니 6 이 나았다. 8장 기준 **WebP 를 세 지표 모두, JPEG XL 을 PSNR 로** 넘었다: WebP -5.9% / +3.9% / **-0.9%**, JXL **-0.8%** / +18.0% / +31.5%. AVIF 는 +69.7% / +70.8% / **+41.9%**. 여기까지 **디코더 줄 수는 그대로 1,303 이다** |
| 2026-08-22 | 왜곡 자에 **블록 경계 가중** (인코더 전용) | 블록의 끝 열·끝 행은 다음 블록의 예측 이웃이 된다. 거기가 틀리면 오차가 옆으로 번지는데 블록 하나만 보는 자는 그것을 못 본다. 32/16 배로 세니 전 칸이 조금씩 좋아졌다 |
| 2026-08-22 | **계수 크기 꼬리에 모델 붙임** (깃발 2개 → 3개 + 지수 골롬 접두 모델) | 전체 비트의 **38.8%가 모델 없이 반반으로** 나가고 있었다. 거기에 모델을 붙이니 **모든 칸이 2~3.6%p 뛰었다**: 8장 기준 JPEG **-26.4 / -24.4 / -15.4%**, WebP **-9.5 / -0.5 / -4.4%**(세 지표 완승), JXL **-4.1** / +13.8 / +27.3%, AVIF +64.8 / +65.8 / **+38.2%**. 디코더 1,303 → 1,328 줄 |
| 2026-08-22 | **계수 무리를 2차원 이웃으로** (이웃 단계 5) | 지그재그 순서에서 바로 앞 두 계수를 보던 것을, 블록 안 자리 기준으로 왼쪽·위·왼쪽위 셋을 보게 바꿨다. 지그재그는 대각선을 번갈아 훑으므로 순서상 앞이라고 블록 안에서 옆인 것이 아니다 — 대각선을 꺾는 자리에서는 먼 계수를 이웃이라 부르고 있었다. 이웃 표본이 세 배로 늘어 단계도 셋에서 다섯으로 늘릴 수 있게 됐다(1차원이던 때는 다섯이 나빴다). 8장 기준 JPEG **-27.5 / -25.4 / -16.7%**, WebP **-10.7 / -1.5 / -5.8%**, JXL **-5.5** / +12.2 / +25.2%, AVIF +62.2 / +63.1 / **+36.1%** |
| 2026-08-22 | **시작 확률표를 학습해서 깖** (693 칸, 1.4KB) | 조각마다 확률이 반반에서 출발해 조각이 끝날 때쯤 자리를 잡고, 다음 조각에서 다시 반반으로 돌아가고 있었다. 표준 사진 8장을 품질 네 단계로 담아 조각 끝의 확률을 모아 평균했다. **압축률과 조각 독립의 대가를 함께 샀다**: 8장 기준 JPEG **-28.1 / -26.1 / -17.5%**, WebP **-11.2 / -2.1 / -6.6%**, JXL **-6.4** / +11.4 / +24.0%, AVIF +59.9 / +60.7 / **+33.6%**. 조각 64 의 대가가 29.7% → **22.3%**, 기본값 256 은 2.7% → 2.4% (**08-23 정정**: 그 뒤 부호기가 크게 바뀌어 이 개선이 남아 있지 않다. 다시 재니 64 가 +29.81%, 256 이 +2.86% 로 확률표 이전 값으로 돌아왔다) |
| 2026-08-22 | (되돌림, 두 번째) 블록마다 양자화 세기 조절 | 자를 고친 뒤 다시 쟀다. 평탄한 블록을 거칠게 가면 PSNR 은 -31.2% → **-32.1%**, SSIM 은 -29.0% → -29.6% 로 좋아지는데 **SSIMULACRA2 가 -22.3% → -20.1%** 로 나빠진다. 제곱 오차는 줄어도 사람 눈에 보이는 뭉갬이 생긴다. 반대 방향(평탄을 곱게)은 세 지표 모두 손해다 |
| 2026-08-22 | **모드 비트에 앞 블록의 모드를 문맥으로** (확률표 재학습) | 이웃한 블록은 대개 같은 방향으로 예측된다. 하늘이 이어지면 옆도 세로, 벽이 이어지면 옆도 가로다. HEVC·AV1 의 most probable mode 와 같은 생각이다. 문맥이 3 에서 12 로 늘어 확률표도 다시 학습했다. 8장 기준 JPEG **-28.4 / -26.3 / -17.7%**, WebP **-11.4 / -2.3 / -6.8%**, JXL **-6.6** / +11.0 / +23.7%, AVIF +59.1 / +59.9 / **+32.8%** |
| 2026-08-22 | (되돌림) 나눔 비트에 앞 블록의 나눔 여부를 문맥으로 | 확률표를 다시 학습해도 전 칸이 0.1~0.3%p 씩 나빠졌다. 모드는 이웃과 닮는데 나눔은 안 닮는다 — 나뉜 자리 옆이 평탄한 경우가 흔하다. 문맥만 늘어 학습이 희석됐다 |
| 2026-08-22 | (보류) 여러 값을 한 번에 담기 (다심볼 부호기) | 만들어서 왕복까지 통과시켰다(누적 확률표 15비트, 갱신률 5, 심볼 넷). 결과는 **압축률 0.7% 손해, 디코딩 8% 이득**이다(0.150 → 0.138초). AVIF 와 지각 지표 33% 차이인 지금은 압축률이 급하므로 안 넣는다. 속도 축을 되찾을 때 다시 꺼낸다. 만들면서 얻은 것: 누적 확률표는 칸 간격이 0 이 되면 범위가 0 이 되어 정규화가 영영 안 끝난다 — 갱신 뒤 칸마다 최소 하나를 남겨야 한다 |
| 2026-08-22 | **머리말의 예약 32비트를 손상 감지 해시로** | 산술 부호화는 어떤 비트열도 그럴듯한 값으로 읽어 낸다. 그래서 망가진 파일이 조용히 딴 그림이 되고 있었다 — 손상 300건 중 **44건**이 그림을 냈고 40건이 20 dB 아래, 최악 11.55 dB 였다. 이미 0 으로 비어 있던 자리를 쓰므로 **크기가 한 바이트도 안 늘고** 조용한 오답이 0 건이 됐다. 옛 판과 호환되지 않는다 |
| 2026-08-22 | 디코더: 화소 버퍼 전에 목차를 먼저 검증 | 1KB 짜리 파일이 65535×1524 를 주장하면 600MB 를 물렸다. 남의 파일을 받는 곳에서는 그것이 곧 서비스 정지다 |
| 2026-08-22 | **규격서를 코드에 맞춤 (아홉 자리)** | 전수 대조에서 갈린 자리가 아홉이었다. 역변환 행렬 방향(문서는 같은 방향, 코드는 전치 — 그대로 만들면 34.59 → 9.94 dB), 시프트의 음수 반올림(미기재, 0 쪽 자름으로 읽으면 91.5% 상이), 평면 예측 기울기 눈금이 정확히 절반(출력 40% 상이), 이웃 개수 8 고정, 시작 확률 반반, `last` 상한, 모드 문맥 미기재, 모델 총수 24 대 702, 무리 산식 부재. **남이 규격서만 보고 만든 디코더가 우리 파일을 한 장도 못 읽었다** |
| 2026-08-23 | **인코더 수리: 모드 값을 후보마다 실측** (문법 무변경) | 모드를 고른 **뒤에** 상수 2비트를 더하고 있었다. 후보 사이의 차이가 0 이므로 모드 선택이 왜곡만 보고 결정됐다 — 어제 「예측 모드 여덟」이 세 번 다 진 이유가 여기 있다. 확률 모델로 후보마다 값을 매기니 세 지표가 함께 좋아졌다: PSNR **-0.66%**, SSIM -0.44%, SSIMULACRA2 -0.28% |
| 2026-08-23 | **모드를 넷 다 담아 봄** (인코더 전용, 2 → 4) | 잔차 절대합 순위로 위 둘만 담아 보고 있었다. 순위는 담는 값을 모르므로 자주 틀린다 — 잔차가 조금 큰 모드가 확률 모델에서 훨씬 싸게 담기는 일이 흔하다. 누적 PSNR **-2.13%**, SSIM -1.68%, SSIMULACRA2 -1.80%. 인코딩 시간이 늘어나는 손잡이다 |
| 2026-08-23 | **확실한 깃발을 안 적는다** (지그재그 `last-1` 자리) | `last` 가 「마지막 비영 계수의 자리 + 1」이므로 그 자리의 계수는 **정의상 0 이 아니다.** 그런데 「0 인가」 깃발을 매번 적고 있었다. 실측으로 그 자리 **100.0000%** 가 「0 아님」이었고, 품질 40 이면 전체 깃발의 **18.1%** 가 그 자리였다. 답이 정해진 질문에 비트를 내고 있었다. 깃발 하나를 건너뛰니 누적 PSNR **-4.72%**, SSIM **-4.23%**, SSIMULACRA2 **-5.20%**. 확률표를 늘리지 않는다 — 건너뛴 뒤의 깃발은 두 경로에서 조건이 완전히 같다 |
| 2026-08-23 | (되돌림) 블록마다 λ 조절 (신호 없음) | 사람 눈이 무늬 뒤의 오차를 덜 보는 것을 노렸다. 분산의 제곱근으로 λ 를 조절했고 **양쪽 방향 모두 손해**다: 거친 블록에서 아끼면 세기 8·16·24 에서 SSIMULACRA2 +0.66 / +7.19 / +12.20%, 반대 방향도 지금 기본값보다 +0.42%p 나쁘다. 어제 양자화 단계를 바꾸는 방식으로 두 번 졌던 것과 방향이 같다. 이 축은 닫는다 |
| 2026-08-23 | (안 넣음) 부호에 문맥 모델 | 부호가 전체 비트의 **12~18%** 인데 전부 반반으로 나간다. 대역·평면·이웃 계수 부호·크기까지 문맥을 넣어 조건부 엔트로피를 재니 최대 **0.55%** 였다(품질 40 기준, 낮은 품질에서는 0.18%). 문맥 넷을 디코더에 넣을 값어치가 아니다 |
| 2026-08-23 | **시작 확률표 재학습** (깃발 건너뛰기 반영) | 깃발 0 이 보는 표본이 바뀌었으므로 다시 배웠다. 누적 PSNR **-4.85%**, SSIM **-4.27%**, SSIMULACRA2 **-5.29%** |
| 2026-08-23 | (되돌림) 계수 크기를 율왜곡으로 하나 낮추기 (인코더 전용) | 크기 3 을 2 로 내리면 깃발이 덜 드니 그 값을 λ 로 견주게 했다. 변환 영역 제곱오차를 화소 눈금으로 옮기는 배수는 행렬 열 크기 256 과 시프트에서 정확히 나온다(4·8 은 16 배, 16 은 4 배). 결과는 **PSNR -4.85 → -4.23%, SSIM -4.27 → -3.80%, SSIMULACRA2 -5.29 → -1.66%.** 지각 지표가 3.6%p 무너진다 |
| 2026-08-23 | **(축을 닫음) 계수 정밀도를 비트와 맞바꾸는 갈래** | 꼬리 계수 자르기(두 번), 블록마다 양자화 세기 조절(두 번), 블록마다 λ 조절, 계수 크기 율왜곡 낮추기 — **여섯 번 재고 여섯 번 다 SSIMULACRA2 가 무너졌다.** PSNR 은 좋아지는 경우가 있어 제곱 오차만 보면 이득으로 보인다. 우리 양자화가 이미 지각상 맞는 자리에 있고, 제곱 오차를 보는 율왜곡 판단은 세부를 지우는 쪽으로 체계적으로 지나치게 간다. 이 축은 더 안 판다 |
| 2026-08-23 | (안 넣음) 평면 밖 블록에 아무것도 안 적기 | 16×16 이 이미지 끝에 걸쳐 나뉘면 하위 블록 일부가 평면 밖으로 통째로 나간다. 거기에도 모드와 `last` 를 적고 있었다. 고쳐서 재니 **-0.06 / -0.08 / +0.01%p** 로 사실상 0 이다. 이유는 따로 잰 숫자가 말해 준다 — **분할 자체가 거의 안 일어난다**(q20 에서 16×16 단위 15,486 개 중 나뉜 것이 10 개, q63 은 0 개). 나뉘지 않으면 평면 밖 하위 블록도 없다. 규격을 가르고 얻는 것이 없으므로 안 넣는다 |
| 2026-08-23 | (유지) 데드존 6 재훑기 | 부호기가 오늘 크게 바뀌었으므로 다시 훑었다. 확정판 기준 **6 이 -7.18 / -7.68 / -10.30, 7 이 -5.03 / -5.38 / -8.69, 8 이 +1.96 / +1.29 / -3.39** 다. 양쪽이 다 나빠 6 이 굳었다 |
| 2026-08-23 | **블록 경계 필터** (flags 비트 2, 디코더 전용) | 어제 「조각 뒤처리 필터」로 세 세기 모두 졌던 도구다. 진 이유가 필터가 아니라 **심판**이었다 — 그 실험은 08-21 16:08 이고 지각 지표는 VMAF 가 21:21, SSIMULACRA2 가 23:16 에 붙었다. 평활 필터를 가장 못 보는 두 자(PSNR·SSIM)로만 재고 기각했다. 그리고 그때 필터는 평탄 판정도 모서리 판정도 없는 단일 세기였다. HEVC 식으로 다시 짜서 재니 누적 **PSNR -7.18%, SSIM -7.68%, SSIMULACRA2 -10.30%** 이고, 필터 몫만 **-2.33 / -3.41 / -5.01%p** 다(관여하지 않은 판정자가 `-DINGOT_LF=0` 으로 따로 확인). **비트를 한 바이트도 안 쓴다** — 같은 파일, 디코더만 다르다. 우리 결함이 실제로 블록이라는 것도 쟀다(16격자 위 차이가 원본 대비 q20 +133%, q63 +571% 초과). 예약 비트를 처음 쓴 사례이고, 새 파일은 옛 디코더가 「예약 비트 1」로 깨끗이 거절한다. 디코더 +124 줄 |
| 2026-08-23 | (반증) 「이웃 화소를 다듬는 쪽에 답이 있다」 | 08-21 이력에 내가 적어 둔 추측이다. 한쪽만 보는 이웃 평활을 실제로 만들어 재니 문턱 4·8·16 **전부에서 진다**(+0.19 ~ +0.68%). 답은 예측에 먹이는 평활이 아니라 출력에 거는 필터였다. 양쪽을 보는 필터는 원리상 다음 블록의 예측 이웃이 될 수 없다 — 블록의 왼쪽 경계를 펴려면 그 블록 자신의 화소가 있어야 하기 때문이다 |
| 2026-08-23 | (안 넣음) 구별되지 않는 모드에 비트를 안 내기 | 조각의 맨 위 줄에서는 세로·평면이 DC 와 같은 그림이라 넷 중 둘만 서로 다르고, 왼쪽 위 모서리는 전부 같다. 그 자리에서 1 비트·0 비트로 줄여 보았다. 양쪽 다 확률표를 다시 배워 견주니 **켠 판 -7.16 / -7.68 / -10.36, 끈 판 -7.18 / -7.68 / -10.30** 으로 사실상 동률이다. 이유가 흥미롭다 — 옛 방식이 비트를 낭비하고 있던 것이 아니었다. 같은 그림을 내는 모드가 여럿이면 인코더가 **그중 확률 모델에서 가장 싼 것을 고를 수 있고**, 그 자유가 목록을 줄여 얻는 만큼을 이미 벌고 있었다. 확률표 4 칸과 디코더 분기를 살 값어치가 아니다 |
| 2026-08-23 | (유지) 왜곡 자의 경계 가중 재훑기 | 필터가 경계 오차를 치워 주니 인코더가 경계에 내던 웃돈이 이제 낭비일 것이라 보았다. 확정판에서 다시 훑으니 아니었다: 16(끔) -7.10 / -7.51 / -10.22, **32(현재) -7.16 / -7.68 / -10.36**, 48 -7.19 / -7.74 / -10.23. 48 이 제곱 오차로 조금 낫지만 지각 지표를 0.13%p 잃는다. 32 를 유지한다 |
| 2026-08-23 | **(측정 방법 정정) BD-rate 적분에 화질 바닥** | 겹치는 구간 전체를 균등 적분하고 있었다. 그 안에 품질 63(화소당 0.05 비트) 같은 아무도 안 쓰는 구간이 들어 있었고, **그 한 칸이 우리끼리 견주는 이득의 28% 를 만들고 있었다.** PSNR 30 dB · SSIMULACRA2 30 점 아래를 안 재도록 바닥을 넣었다. 오늘 판의 이득이 **-7.18 / -7.68 / -10.30 에서 -5.20 / -7.68 / -5.78** 로 내려간다. 바깥 코덱과의 표는 0.0~4.5%p 만 움직이고 가장 큰 변동은 AVIF 지각 격차가 우리에게 유리한 쪽이다. **이 줄 앞뒤의 우리끼리 숫자는 직접 비교하면 안 된다** |
| 2026-08-23 | **(정정) 속도 표와 화질 표가 서로 다른 AVIF 를 재고 있었다** | `tools/speed.py` 가 libaom 을 `-cpu-used` 없이 부르고 `tools/bench.py` 는 `-cpu-used 6` 으로 불렀다. 설정을 손으로 두 벌 베껴 둔 탓이다. 맞춰 재니 **AVIF 인코딩 0.280 초 대 우리 2.648 초로 우리가 9.5 배 느리다.** README 가 「AVIF 보다 빠르다」고 적고 있던 것이 틀렸다. 속도 도구가 화질 도구의 설정을 직접 읽게 고쳤다 |
| 2026-08-23 | (유지) 고주파 양자화 가중치 1 재심 | 이 값을 1 로 묶어 둔 근거가 「2 로 가면 PSNR 로 JXL 을 잃는다」였는데 그 제약이 오늘 풀렸다(JXL PSNR 격차가 +0.1% 에서 -10.9% 로). 그래서 다시 쟀다. **적분 바닥을 넣기 전 자로는 2 가 지각 지표를 3.32%p 올리는 것으로 나왔다.** 바닥을 넣고 같은 조건에서 다시 재니 **세 지표 모두 나쁘다**: 가중 1 이 -5.20 / -7.68 / -5.78, 가중 2 가 -0.49 / -4.68 / -3.80. 그 이득은 아무도 안 쓰는 저화질 구간이 만들고 있었다. **두 숫자는 자가 달라 나란히 쓰면 안 된다.** 1 을 유지한다 |
| 2026-08-23 | **변환 눈금을 세 크기에 맞춤** (16×16 만 15/17 이었다) | 상수 잔차 100 의 DC 를 직교 기준으로 나누면 배수가 4.00 / 4.04 / **2.00** 이었다. 같은 양자화 스텝이 16×16 에서만 두 배 거칠게 들었고, 인코더가 「16 을 넷으로 나눌까」를 견줄 때 두 후보가 서로 다른 세기를 받아 그 판단이 죽어 있었다. 왕복 오차가 8×8 의 두 배였던 것도 같은 이유다. 14/18 로 맞추니 같은 조건에서 **-5.20 / -7.68 / -5.78 이 -7.78 / -9.86 / -6.63** 이 된다. **디코더 줄은 안 늘고 상수 두 개만 바뀐다** |
| 2026-08-23 | **품질 눈금 배수 16 → 32** (위 변경과 한 몸) | 변환 눈금을 맞추면 계수가 두 배 커져 같은 품질 번호가 훨씬 고화질을 뜻하게 된다(q20 이 옛 q10 쯤). 그대로 두면 사용자 기대도 벤치 곡선도 어긋나므로 스텝을 두 배로 되돌린다. Baruch q10 에서 **2.104 bpp / 30.80 dB 대 옛 판 2.105 bpp / 30.68 dB** 로 눈금이 겹치면서 화질만 0.12 dB 높다. **이 둘은 따로 넣으면 안 된다** — 변환만 바꾸면 눈금이 어긋나고, 눈금만 바꾸면 그냥 품질이 내려간다 |
| 2026-08-23 | **(되돌림, 여섯 번째) 방향 예측 넷 추가 (모드 8)** | 앞선 다섯 번은 인코더의 자가 고장 난 상태에서 잰 것이라, 자를 고친 판에서 다시 걸었다. 각도표 + 각도를 매개변수로 받는 예측기 하나 + 16×16 참조 평활 구조이고, **켠 판과 끈 판 양쪽 다 확률표를 따로 배워** 견줬다. 왕복 11/11 과 손상 300건 조용한 오답 0 도 통과했다. 그런데도 **세 지표 모두 나쁘다**: PSNR **+1.43%**, SSIM **+2.62%**, SSIMULACRA2 **+0.38%**. 이번에는 자가 멀쩡했고 표도 공정했으므로 「잘못 쟀다」는 설명이 안 남는다. **모드를 늘리는 갈래는 여섯 번 재고 여섯 번 다 졌다.** 우리 계수 모델이 이미 방향성 잔차를 싸게 담고 있어서, 예측을 더 맞혀도 아끼는 비트보다 모드 기호가 비싸지는 쪽이 크다고 본다(추정). 손잡이 `INGOT_DIRPRED` 를 꺼진 채로 남긴다 |
| 2026-08-23 | **목차에서 오프셋 칸을 뺌** (조각당 8 → 4 바이트) | 오프셋은 앞 조각들의 길이 합이라 디코더가 이미 안다. 인코더가 조각을 마지막에 붙여 모으므로 파일 안에 빈틈이 없기 때문이다. 조각당 4 바이트를 그냥 버리고 있었다. 세 지표가 함께 올랐다: **-0.24 / -0.30 / -0.18%**. 디코더는 누적 변수 하나로 두 줄 는다. 조각을 아무 순서로나 병렬로 푸는 성질은 그대로다 — 자리를 미리 다 계산할 수 있다 |
| 2026-08-23 | **(되돌림) 방향 예측 + 예측 모드에 맞는 변환(ADST) 을 같이 넣음** | 이력의 두 기각이 서로를 이유로 대고 있었다. ADST 는 「우리는 모드가 넷뿐이라 쓸 자리가 적다」(08-21), 방향 예측은 「자를 고치고 표까지 양쪽 재학습해도 진다」(08-23, 여섯 번째)다. 하나만 넣는 시험으로는 그 고리를 못 끊으므로 네 조합을 **조합마다 확률표를 따로 배워** 한 자로 쟀다 (표준 사진 8장, 적분 바닥 있음, 대조군 = 손잡이 둘 다 꺼짐). 방향만 +1.45 / +2.64 / +0.39, 변환만 +1.70 / +2.41 / +2.73, **둘 다 +5.25 / +7.81 / +5.11**. 배정을 가장 얌전하게(세로·가로 예측에만) 잡아도 +1.45 와 +1.12 에 **둘 다가 +2.58** 로 두 손해의 합이다 — 서로 갚아 주는 자리가 없다. 왕복 11/11·거절 10/10·손상 300건 조용한 오답 0 은 네 판 다 통과한다. **덤**: 방향을 일부러 뒤집어 붙인 판이 제대로 붙인 판보다 여덟 장 중 일곱 장에서 0.35%p 덜 진다. 변환 자체는 제대로 붙었다(무작위 걸음 잔차에서 문턱 넘는 계수 5.38 → 5.13 개). ADST 가 우리 잔차에서 이론이 말하는 일을 안 한다고 본다(추정) — DCT 가 잘 담던 축에 걸면 더 잃고 이미 못 담던 축에 걸면 덜 잃는다. 손잡이 `INGOT_ADST`(기본 0, 배정은 `INGOT_ADST_MAP`)를 꺼진 채로 남긴다. 켜면 디코더 쪽 줄이 2,191 → 2,307 로 는다 |
| 2026-08-23 | **(측정기 수리) 문맥 배치가 바뀌면 컴파일을 멈춘다** | 학습 확률표 702 칸은 지금 문맥 배치에 맞춰 배운 것이다. 배치를 바꾸는 손잡이(`INGOT_NBLEV`·`INGOT_CTX_BYSIZE`)를 돌리면 칸 번호가 밀려 표가 엉뚱한 자리에 깔리는데, 그것이 그 손잡이의 손해로 잡힌다. 실측으로 NBLEV=7 이 **3.53%p**, BYSIZE=0 이 **4.56%p** 의 가짜 벌점을 받고 있었다. `#error` 로 막았다 — 조용히 틀리느니 안 돌게 한다 |
| 2026-08-23 | **(측정기 수리) 시험 이미지 목록을 파일로 고정** | 30 장 중 앞 여덟 장을 쓰고 있었는데 고른 것이 아니라 가나다순 앞자리였고, 그 여덟이 유별나게 거칠다. 같은 변경을 30 장으로 재면 답이 **최대 두 배** 어긋난다(부호는 안 뒤집혔다). 같은 자르기가 도구 일곱 군데에 흩어져 있었다. `testdata/SET.txt` 를 정본으로 두고 왜 그 장인지 적었다 |
| 2026-08-23 | (되돌림) 나눔 비트의 값을 후보마다 실측 (인코더 전용) | 아침에 모드 기호에서 같은 결함을 고쳐 -0.66% 를 벌었으므로 나눔 비트에도 걸었다. 나눌 때와 안 나눌 때 모두 상수 한 비트로 치고 있었으니 후보 사이 차이가 0 이었다. 그런데 **+0.12 / +0.13 / +0.02 로 진다.** 이유는 따로 잰 숫자가 말해 준다 — **나뉜 자리가 8장 통틀어 q20 에 261개, q63 에 16개뿐이다.** 분할이 거의 안 일어나니 그 비트를 정확히 매겨도 잴 것이 없고, 확률 모델을 읽는 값만 남는다. 같은 결함이라도 그 기호가 자주 나와야 고칠 값어치가 있다 |
| 2026-08-23 | (안 넣음) 고주파 양자화 가중을 1/16 눈금으로 1.75 | 정수 손잡이라 1 과 2 사이를 못 짚는 것이 문제라 보고 1/16 눈금으로 다시 쟀다. **지각 -3.83% 인데 PSNR +2.59%, SSIM +1.17%** 다. 이득이 아니라 거래다. **AVIF 가 우리를 세 지표 모두에서 이기는 지금은 한 축을 사고 다른 축을 파는 것이 앞으로 못 간다** — 지각 격차만 줄고 제곱 오차 격차가 벌어진다. 세 지표가 함께 오르는 것만 넣는다. 이 값은 AVIF 를 PSNR·SSIM 으로 따라잡은 뒤에 다시 꺼낸다 |
| 2026-08-23 | **조각마다 나가던 첫 0 바이트 제거** | 레인지 코더가 시작할 때 캐시를 하나 차 있다고 보므로 첫 밀어내기에서 언제나 0 한 바이트가 먼저 나갔고, 디코더는 다섯 바이트를 읽어 그중 첫 바이트를 32비트 code 밖으로 밀어 버렸다. **조각마다 1 바이트를 그냥 버리고 있었다.** 조각이 많을수록 커진다 — 품질 63 에서 파일의 0.46% 였다. 첫 밀어내기에만 표시를 두고 건너뛴다(그때는 캐시가 비어 있고 자리올림도 없어 0xFF 대기 갈래를 안 깬다). 디코더는 다섯 바이트 대신 넷을 읽는다. **-0.06 / -0.08 / -0.05%**. 세 지표가 함께 오른다 |

| 2026-08-23 | **왜곡 자의 경계 가중을 그림 격자로** (인코더 전용) | 가중을 블록의 끝 열·행에 매기고 있었다. 그러면 16×16 을 넷으로 나눈 후보는 안쪽에도 8×8 마다 끝이 생겨 가중받는 화소가 통짜 후보의 두 배가 된다. **후보의 모양이 자를 바꾸면 그 비교는 공정하지 않다** — 나눔이 실제보다 비싸게 매겨지고 있었다. 그림 안 16 격자로 옮기니 어느 후보를 재든 같은 화소가 가중된다. **-0.13 / -0.03 / -0.02%**. 규격이 아니라 인코더의 판단이므로 디코더는 그대로다 |
| 2026-08-23 | **기본 조각 크기 256 → 512** | 첫 커밋(08-21)에서 정한 뒤 한 번도 다시 안 정한 값이다. 그때 믿던 조각 대가는 나중에 **21 배 틀린 것**으로 밝혀졌으므로([구조를 바꾸면 옛 측정이 무효] 항목) 다시 걸었다. **-1.02 / -1.48 / -0.79%** 로 오늘 넣은 것 중 가장 크다. 조각마다 확률 모델과 레인지 코더가 처음부터 다시 시작하므로, 조각이 클수록 그 준비 비용을 덜 낸다. **대가는 병렬 알갱이다** — 968×1188 그림에서 조각이 20 개에서 6 개로 줄어 코어를 그만큼 덜 쓴다. 규격이 아니라 인코더의 기본값이므로 병렬이 급한 쪽은 `INGOT_GROUP` 으로 256 을 그대로 쓸 수 있고, 디코더는 어느 쪽이든 읽는다 |

| 2026-08-23 | **조각 끝의 마무리 꼬리를 자름** | 레인지 코더는 끝낼 때 다섯 바이트를 밀어냈다. 그런데 마무리 직전에 low 를 2^24 배수로 올리면 아래 세 바이트가 0 이 되고, 올린 양이 2^24 미만인데 정규화 뒤 range 는 항상 2^24 이상이므로 **그 값은 여전히 인코더가 뜻한 구간 안이다** — 디코더가 읽는 답이 안 바뀐다. 그렇게 생긴 0 꼬리를 안 낸다. 디코더는 조각 끝을 넘겨 읽을 때 이미 0 을 채우고 있었으므로 고칠 것이 없고, 그것을 손상으로 치던 표시만 꼬리 길이(5)만큼 봐준다. 진짜 잘린 파일은 해시가 잡는다. 조각마다 3 바이트이고 품질 63 의 Baruch 에서 파일의 **0.80%** 였다. **-0.13 / -0.34 / -0.65%** |
| 2026-08-23 | **(측정기 수리) 벤치가 인코더의 기본 조각 크기를 안 봤다** | `bench.py` 가 조각 크기를 언제나 명령행 인자로 넘겼고 그 값이 `"8"` 로 박혀 있었다. 그래서 소스의 기본값을 512 로 바꿔도 벤치는 256 으로 쟀고, `-DINGOT_GROUP_DEFAULT=10` 이 **정확히 +0.00** 으로 찍혔다. 손잡이가 진 것이 아니라 **안 닿은 것**이다. 같은 결함이 `learn_probs.py`·`speed.py`·`certain_flag.py` 에도 있어서 **확률표를 조각 256 으로 배워 놓고 512 로 쓰고 있었다.** 넷 다 `"0"`(= 인코더 기본값)을 넘기게 고쳤다. 정확히 +0.00 은 「변화 없음」이 아니라 「못 쟀다」로 읽는다 |
| 2026-08-23 | (닫음) 부호를 문맥으로 줄이는 갈래 | 부호는 파일의 15~21% 인데 반반 비트로 쓰고 있어서 큰 자리로 보였다. 사진 3장 x 품질 2 의 비영 계수 176 만 개를 이웃 계수(왼쪽·위)의 부호 합으로 갈라 세니 **평균 0.9993 비트**다. 색차는 치우침이 보이지만(이웃 -1 에서 양수 60.9%) 양이 전체의 1% 미만이다. 문맥 모델로는 부호 비트의 **0.07%** 밖에 못 줄인다 — 모델을 붙일 값이 없다. 줄이려면 모델이 아니라 **안 적는** 쪽이어야 한다 |

| 2026-08-23 | **(결함 수리) 꼬리 자르기가 정상 파일을 거절했다** | 넣은 지 한 시간 만에 드러났다. **968x1188 실사진의 품질 40 이상이 전부 안 열렸다.** 잘리는 0 이 마무리가 낸 다섯 바이트만이 아니라 그 앞의 코딩 바이트까지 포함하는데(품질 63 의 Baruch 는 129 바이트, 조각당 평균 21), 디코더가 봐주는 길이를 다섯으로 묶어 두어 여섯 번째 읽기부터 손상으로 판정했다. 조각 끝을 넘겨 0 을 읽는 것은 정상이므로 세지 않게 고쳤다 — 정상 파일에서 디코더가 인코더가 낸 길이를 넘겨 읽는 일은 없고, 잘린 파일은 해시가 잡는다. **왜 안 걸렸나**: 시험 그림이 작고 무늬가 고르면 조각당 두세 바이트만 잘려 안 걸린다. 잘리는 양은 조각 끝이 얼마나 조용한가에 달렸는데 그 조건을 만드는 그림이 목록에 없었다. 게다가 벤치가 우리 코덱의 실패를 조용히 건너뛰어, 품질 40·63 이 통째로 빠진 채 순위표가 나왔다. **셋 다 고쳤다** — 시험에 「조각 높이마다 잡음 띠 + 단색」인 1024x1536 세 건(깨진 판에서 3건 실패), 벤치는 실패한 점을 세어 알린다 |

| 2026-08-23 | (되돌림) **부호 감추기** — 마지막 비영 계수의 부호를 홀짝으로 알림 | 부호는 파일의 15~21% 인데 반반 비트로 쓰고 있어서 큰 자리로 보였다. 문맥 모델은 절약 여지가 0.07% 뿐이라(같은 날 실측) 모델 대신 **안 적는** 쪽을 걸었다. 마지막 비영 계수의 부호를 빼고 계수 절댓값 합의 홀짝으로 알리며, 홀짝이 안 맞으면 계수 하나를 ±1 옮겨 맞춘다. 왕복은 정확했고 파일도 작아졌다(품질 63 의 Baruch 에서 8.9%). 그런데 **-0.10 / +0.38 / +2.63** 이다. ±1 이 세부를 지우는 대가가 아낀 한 비트보다 크다. 왜곡만 보고 옮길 자리를 고르면 파일이 오히려 커져서 비트 값까지 넣어 골랐는데도 그렇다. **이로써 부호를 줄이는 두 갈래가 다 닫혔다.** 손잡이 `INGOT_SIGNHIDE` 를 꺼진 채로 남긴다 |

| 2026-08-23 | **고주파 양자화 가중을 블록 크기에 맞춤** | 가중이 `u+v` 를 그대로 쓰고 있었다. 그런데 같은 u 라도 뜻하는 주파수가 크기마다 다르다 — 8×8 의 u=4 와 16×16 의 u=8 이 같은 물리 주파수다. 그래서 **작은 블록일수록 고주파를 덜 거칠게** 다뤘고(자리 최댓값이 4×4 는 6, 8×8 은 14, 16×16 은 30), 인코더가 「16 을 넷으로 나눌까」를 견줄 때 두 후보의 양자화 세기가 서로 달랐다. 자리를 16 눈금으로 옮겼다: `(u+v)*16/n`. 16×16 은 옛 판과 똑같고 나머지가 같은 자로 맞춰진다. **-3.02 / -2.46 / -2.01%** 로 그날 가장 큰 한 수다. 오늘 같은 계열을 셋 찾았고(변환 눈금·경계 가중·이것) 셋 다 이겼다 — **후보의 모양이 자를 바꾸면 그 비교는 공정하지 않다** |
| 2026-08-23 | **λ 10 → 3** (인코더 전용) | 비트 하나를 왜곡 얼마와 맞바꿀지 정하는 값이다. 8/22 에 λ 산식의 정밀도 결함을 고친 뒤 한 번도 다시 안 정했다. 여섯 값을 재니 6 이 -0.87 / -0.55 / -0.25, 5 가 -1.39 / -0.89 / -0.36, 4 가 -2.03 / -1.40 / -0.47, **3 이 -2.91 / -2.22 / -0.36**, 2 가 -4.78 / -3.17 / **+2.08**, 1 이 -5.91 / -3.52 / **+7.48** 이다. 낮출수록 인코더가 비트를 더 쓰는 쪽으로 기울어 제곱 오차는 계속 좋아지지만 **지각 지표가 4 에서 바닥을 치고 급격히 무너진다.** 세 지표가 함께 오르는 마지막 지점인 3 을 고른다 |
| 2026-08-23 | **가장 큰 블록을 16×16 → 32×32** | AVIF 는 32·64 까지 쓰는데 우리는 16 에서 멈춰 있었다. 매끈한 자리에서는 큰 변환 하나가 작은 변환 넷보다 싸다. 32 점 정수 DCT 표와 1024 칸 지그재그를 넣었고(`tools/gen_dct.py` 가 만든다. 같은 규칙이 소스의 4·8·16 표와 지그재그를 그대로 재현하는 것을 확인했다), 시프트는 15/19 로 상수 블록의 DC 가 직교 기준의 4.00 배가 되게 맞췄다 — 16×16 과 같은 값이라 같은 양자화 스텝이 같은 세기를 뜻한다. 문맥 칸이 702 → **719** 로 늘어(크기 무리 하나 + 나눔 단계 하나) 표를 다시 배웠다. **양쪽 다 확률표를 따로 배워** 재니 **-2.86 / -3.90 / -3.07%** 다. **대가**: 왕복 오차가 최대 5·제곱평균제곱근 1.27 에서 6·1.61 로 늘고, 순변환 누산은 int32 한계의 4.1% 를 쓴다 |
| 2026-08-23 | **(도구 수리) 표를 다시 배우면 가드의 칸 수도 따라온다** | 확률표를 지키는 `#if INGOT_PROB_COUNT != 702` 가 손으로 적은 수였다. 배치를 넓히는 손잡이를 켜고 표를 새로 배우면 표는 719 칸인데 가드는 702 를 들고 있어서 컴파일이 막힌다. `learn_probs.py` 가 표를 심을 때 가드의 수와 오류 문구까지 같이 고치게 했다 |

| 2026-08-23 | **(이겼는데 안 넣음) 64×64 변환** | 32×32 가 이긴 뒤 크기별 선택 빈도를 세니 32×32 가 통짜로 남는 비율이 품질 10 에서 52%, 40 에서 **79.9%** 였다. 큰 쪽일수록 안 나누므로 한 단계 더 걸었다. 64 점 변환은 순변환 2단 누산이 int32 한계의 **394%** 라 그냥은 넘치는데, 1단 결과를 3 비트 미리 내리면 49.2% 로 떨어지고 왕복 오차는 안 내린 판과 같다(무작위 잔차 20 블록, 최대 6·제곱평균제곱근 1.43 — 32×32 의 1.62 보다 낫다). 양쪽 다 확률표를 따로 배워(64 판은 736 칸) 재니 **-0.48 / -0.45 / -0.83%** 로 이긴다. **그런데 안 넣는다** — 인코딩이 7.43 → **15.95 초**(2.15배), 디코딩이 0.322 → **0.544 초**(1.69배)다. 16→32 는 -2.86 / -3.90 / -3.07 을 인코딩 1.7배로 샀으니 값이 있었지만, 32→64 는 -0.5% 대를 2.15배로 사는 셈이다. 디코딩이 1.7배 느려지는 것은 쓰는 사람에게 그대로 간다. 손잡이 `INGOT_BLK64` 를 꺼진 채로 남긴다 |
| 2026-08-23 | (유지) 나눔 조기 종료 문턱 | 문턱을 블록 넓이에 비례시키면 **+0.49 / +0.75 / +0.34** 로 진다. 「크기가 바뀌면 뜻이 바뀌는 값」의 넷째 자리로 보고 걸었는데 계열이 달랐다 — 앞의 셋은 **후보를 견주는 자**였고 이것은 **후보를 안 재고 버리는 문턱**이다. 넓이에 비례시키면 32×32 에서 문턱이 네 배가 되어 나눔 후보를 더 많이 버린다. 문턱을 0 으로 두어 전부 재도 **+0.03 / +0.09 / -0.00** 으로 이득이 없다. 조기 종료가 품질을 안 깎으면서 시험 인코딩만 줄이고 있다. 24 를 유지한다 |
| 2026-08-23 | (유지) 색차 양자화 가중 20 | 고주파 가중 정규화를 넣으면서 이 값이 놓인 자리가 바뀌었으므로 다시 걸었다. 16 은 **+1.79 / +2.13 / +1.66** 으로 지고, 24 는 **+0.06 / +0.21 / -1.04** 로 거래다. 세 지표가 함께 오르는 것만 넣는 기준으로 20 을 유지한다. 다만 24 는 적어 둔다 — JPEG XL 을 세 지표 모두 이기는 것을 목표로 잡으면, PSNR 여유(-18.9%)를 지각과 바꾸는 선택지가 된다 |

| 2026-08-23 | **(인코더만, 규격 무변경) 시험에서 내린 판단을 적어 두고 재생** | 인코더가 같은 나무를 두 번 돌고 있었다 — 값을 재려고 한 번, 이긴 쪽을 쓰려고 또 한 번이다. 깊이 한 단계마다 중복이 곱해져서 잔차 인코딩 횟수가 8×8 은 45, 16×16 은 365, 32×32 는 **2,925**, 64×64 는 23,405 회였다. **시험이 내린 판단은 실제 인코딩이 내릴 판단과 정확히 같다** — 확률 모델도 복원 화소도 시험 전으로 되돌린 뒤 실제 인코딩을 하기 때문이다. 그래서 적어 두고 재생하면 나오는 바이트가 안 바뀐다. 횟수가 25 / 105 / **425** / 1,705 회로 준다. **인코딩 8.47 → 4.54 초(1.86배)**, 그림 넷 × 품질 여섯 **스물네 건 전부 해시가 같다.** 골든도 그대로 통과한다. 모드 재사용이 정말 같은 답인지 따로 계기를 붙여 확인했다 — 품질 4 에서 104,562 건 중 어긋남 0 |

| 2026-08-23 | **이웃 문맥 단계 5 → 7** | 계수의 「0 인가」를 고를 때 쓰는 이웃 크기 합을 몇 단계로 가를지다. 예전에 7 을 재서 **3.53%p 차이로 기각**했는데, 그것은 **확률표가 어긋난 채로 잰 가짜 벌점**이었다 — 이 값을 바꾸면 문맥 칸 번호가 밀려 학습된 표가 엉뚱한 자리에 깔린다(그래서 오늘 오전에 `#error` 로 막았다). 표까지 새로 배워 다시 재니 **-0.09 / -0.05 / -0.05%** 로 이긴다. 벌점이 거의 전부 가짜였다. 대가는 확률표가 719 → **975 칸**으로 커지는 것뿐이다(1.4 KB → 1.9 KB) |
| 2026-08-23 | (되돌림, 재심) 계수 문맥을 블록 크기 넷으로 가름 | 위와 같은 이유로 옛 기각(4.56%p)이 무효라 다시 걸었다. 표까지 새로 배워(1,359 칸) 재니 **+0.10 / +0.13 / +0.03** 이다. 벌점의 4.5%p 는 가짜였지만 **진짜 손해가 남아 있다.** 크기를 넷으로 가르면 무리마다 표본이 줄어 학습이 무뎌지는 쪽이 이득보다 크다고 본다(추정). 재심이 언제나 뒤집기로 끝나지는 않는다 — 재심의 값은 숫자를 바로잡는 것 자체에 있다 |

| 2026-08-23 | **계수 크기 깃발 3 → 5** | 계수의 크기를 적을 때 「1 이상인가」 「2 이상인가」 식으로 깃발을 몇 개까지 두고 그 뒤부터 지수 골롬으로 넘어갈지다. 8/22 에 3·5·8 을 재서 3 을 골랐는데, 그것은 **확률표가 어긋난 채로 잰 값**이었다 — 깃발 수를 바꾸면 모델 자리가 밀려(깃발이 0..RC_FLAGS-1, 지수 골롬이 그 뒤) 배운 표가 엉뚱한 자리에 깔린다. 표까지 새로 배워 다시 재니 **-0.48 / -0.49 / -0.36%** 다. 표 크기는 안 바뀌고 자리의 뜻만 바뀐다. **확률표 어긋남으로 잘못 기각된 세 번째 건이다** |
| 2026-08-23 | (되돌림) 머리말 문맥에 「앞 블록이 얼마나 찼는가」 | 머리말은 품질 40 에서 파일의 11.7% 인데 문맥이 블록 크기와 평면뿐이었다. 앞 블록의 머리말을 넓이 대비 비율로 세 단계로 접어 문맥에 더했다(날값을 쓰면 8×8 의 last=8 과 32×32 의 last=8 이 같은 뜻이 되므로 비율로 맞췄다). **-0.05 / -0.07 / +0.01** 로 이득이 소수점 아래다. 머리말을 적기 전에 이미 **나눔 판단이 앞 블록의 성질을 반영**하고 문맥도 크기별로 갈려 있어서, 같은 신호를 두 번 쓴 셈이라고 본다(추정). 손잡이 `INGOT_LAST_CTX` 를 꺼진 채로 남긴다 |
| 2026-08-23 | (유지) 왜곡 자의 경계 가중 32 | 오늘 바뀐 것들(변환 눈금·λ·32×32·가중 정규화) 뒤에 다시 걸었다. 16 은 **+0.11 / +0.12 / +0.01**, 48 은 **+0.05 / +0.07 / +0.16** 으로 양쪽 다 진다. 32 가 그대로 골짜기 바닥이다 |
| 2026-08-23 | (유지) 블록 경계 필터의 판정·세기 16/8 | 같은 이유로 다시 걸었다. 평탄 문턱 12 는 +0.03 / +0.00 / **-0.12**, 24 는 -0.05 / -0.02 / +0.09, 세기 6 은 +0.12 / +0.15 / **-0.32**, 세기 12 는 +0.68 / +0.38 / +0.60 이다. 넷 다 거래이거나 손해다. **세기 6 과 색차 가중 24 는 적어 둔다** — 둘을 합치면 지각을 약 1.3% 사고 제곱 오차를 0.2~0.4% 파는 카드라, JPEG XL 을 세 지표 모두 이기는 것을 목표로 잡을 때 꺼낸다 |

BD-rate 계산 방법도 이날 바뀌었다. 3차 다항식 전역 적합이 SSIM 곡선에서 발산해(한 조합에서 +103억 %) 단조 보존 보간으로 교체했다. 그 앞뒤 수치는 직접 비교하면 안 된다.
