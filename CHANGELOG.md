# 변경 이력

날짜는 실제 작업일입니다. 첫 공개 전이라 버전 번호를 올리지 않고 이력만 쌓습니다.
비트스트림이 바뀔 때마다 잰 이득을 함께 적습니다.

## v1.8.0 — 2026-08-23 (늦은 밤)

비트스트림이 또 바뀝니다. 옛 파일은 안 읽힙니다.

**측정하는 자를 넷 고쳤습니다. 이것이 이 판의 알맹이입니다.**

- **속도 표와 화질 표가 서로 다른 AVIF 를 재고 있었습니다.** 속도 쪽이 libaom 을 `-cpu-used` 없이 불렀습니다. 설정을 맞추니 **AVIF 가 우리보다 8.4 배 빠릅니다.** README 가 「AVIF 보다 빠르다」고 적고 있던 것은 틀린 말이었습니다. 이제 속도 도구가 화질 도구의 설정을 직접 읽습니다.
- **BD-rate 를 아무도 안 쓰는 화질 구간까지 적분했습니다.** 품질 63(화소당 0.05 비트)이 우리끼리 견주는 이득의 28% 를 만들고 있었습니다. PSNR 30 dB·지각 30 점 아래를 안 재도록 바닥을 넣었습니다.
- **측정 도구가 오늘 판을 안 보고 있었습니다.** 아무것도 안 고쳐도 −7.18% 가 찍혀 실제로 오독이 났습니다.
- **왕복 검사의 화질 바닥이 고장 구간 안에 있었습니다.** 색차를 통째로 버리는 디코더가 통과했습니다. 8.0 을 14.0 으로 올리고, 색차를 안 읽는 디코더를 만들어 열한 사례 전부에서 걸리는 것을 확인했습니다.

**넣은 것 둘.**

**1. 16×16 변환만 눈금이 절반이었습니다.** 같은 양자화 스텝이 16×16 에서만 두 배 거칠게 들어, 인코더가 「16 을 넷으로 나눌까」를 견줄 때 두 후보가 서로 다른 세기를 받았습니다. 그 판단이 사실상 죽어 있었어요. 셋을 맞추니 세 지표가 함께 올랐습니다. 품질 눈금 배수도 16 → 32 로 함께 옮겨야 합니다 — 안 그러면 같은 품질 번호가 다른 화질을 뜻하게 됩니다.

**2. 목차에서 오프셋 칸을 뺐습니다.** 조각당 4 바이트인데 앞 조각들의 길이 합이라 디코더가 이미 압니다. **−0.24 / −0.30 / −0.18%.** 조각을 아무 순서로나 병렬로 푸는 성질은 그대로입니다.

**되돌린 것 — 순환 논리 하나를 닫았습니다.**

이력의 두 기각이 서로를 이유로 대고 있었습니다. ADST 는 「모드가 넷뿐이라 쓸 자리가 적다」, 방향 예측은 「자를 고쳐도 진다」였습니다. 네 조합을 **조합마다 확률표를 따로 배워** 한 자로 쟀습니다.

| 조합 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| 방향 예측만 | +1.45 | +2.64 | +0.39 |
| 변환 종류만 | +1.70 | +2.41 | +2.73 |
| **둘 다** | **+5.25** | **+7.81** | **+5.11** |

**손해가 서로 갚아지는 게 아니라 더해집니다.** 이 갈래는 이제 「하나씩 재서 졌다」가 아니라 「짝으로도 졌다」로 기록됩니다. 변환 방향을 일부러 뒤집은 판이 제대로 붙인 판보다 여덟 장 중 일곱 장에서 덜 지는 것도 덤으로 확인했습니다 — ADST 가 우리 잔차에서 이론이 말하는 일을 안 합니다.

**죽은 발판 359 줄을 걷어냈습니다.** 재서 진 실험의 손잡이들입니다. **빼도 골든이 그대로 일치합니다.** `src/` 3,039 → 2,643 줄, 디코딩 경로 2,315 → 1,978 줄입니다.

## v1.7.0 — 2026-08-23

비트스트림이 바뀝니다. 옛 파일은 안 읽힙니다.

표준 사진 8장 기준, 전날 판 대비 **PSNR −5.20%, SSIM −7.68%, SSIMULACRA2 −5.78%** 입니다.

**이 숫자는 이날 늦게 한 번 내려갔습니다.** 처음에는 −7.18 / −7.68 / −10.30 으로 적었는데, BD-rate 를 겹치는 화질 구간 전체에서 적분하고 있었기 때문입니다. 그 안에 품질 63(화소당 0.05 비트)처럼 아무도 안 쓰는 구간이 들어 있었고 그 한 칸이 이득의 28% 를 만들었습니다. PSNR 30 dB·지각 지표 30 점 아래를 안 재도록 바닥을 넣고 다시 잰 값이 위입니다. **바깥 코덱과의 표는 0.0~4.5%p 만 움직입니다.** 넷이 겹쳐 나온 값이고, 하나씩 적습니다. 아래 각 항목의 숫자는 그 항목까지 쌓인 누적치입니다.

**1. 인코더가 모드 값을 안 보고 있었습니다** (규격 무변경, −0.66 / −0.44 / −0.28%).

모드를 고르는 반복문이 재는 값은 잔차 비트뿐이었고, 모드 자체를 적는 값은 반복문이 **끝난 뒤** 네 모드에 똑같은 상수로 한 번 더해지고 있었습니다. 후보 사이의 차이가 정확히 0 이라, 인코더는 비싼 모드를 공짜로 봤습니다. 규격 이력에 「예측 모드 여덟」이 세 번 적혀 있고 세 번 다 졌는데, 그 자리가 눈이 먼 채로 잰 것이었습니다.

**2. 모드 넷을 다 담아 보게 했습니다** (규격 무변경, 누적 −2.13 / −1.68 / −1.80%).

잔차 절대합으로 순위를 매겨 위 둘만 실제로 담아 보고 있었습니다. 절대합은 변환 뒤의 비트 수와 상관이 약합니다 — 잔차가 조금 큰 모드가 확률 모델에서 훨씬 싸게 담기는 일이 흔합니다. 인코딩이 느려지는 대신 세 지표가 함께 올랐습니다.

**3. 답이 정해진 질문에 비트를 내고 있었습니다** (누적 −4.85 / −4.27 / −5.29%).

`last` 는 「마지막으로 0 이 아닌 계수의 자리 + 1」입니다. 그러므로 지그재그 순번 `last-1` 의 계수는 **정의상 0 이 아닙니다.** 그런데 그 자리에서도 「이게 0 인가」 깃발을 매번 적고 있었습니다. 세어 보니 그 자리 **100.0000%** 가 「0 아님」이었고, 품질 40 이면 전체 깃발의 **18.1%** 가 그 자리였습니다.

깃발 하나만 건너뜁니다. 확률표를 안 늘려도 되는데, 건너뛴 뒤의 깃발이 두 경로에서 뜻이 완전히 같기 때문입니다 — 둘째 깃발은 어느 쪽이든 「`k >= 1` 인 상태에서 `k > 1` 인가」입니다.

찾은 방법도 적어 둡니다. 비트를 갈래별로 세는 계량기를 붙이고, 각 갈래의 조건부 엔트로피를 후보 문맥마다 계산했습니다. 「`last` 까지 남은 거리」를 문맥에 넣으면 2.9~6.5% 가 준다고 나왔고, 칸을 둘로만 줄여도 대부분이 남길래 그 둘이 무엇인지 봤더니 「마지막 자리인가 아닌가」였습니다.

**4. 블록 경계 필터** (flags 비트 2, 디코더 전용, 누적 **−7.18 / −7.68 / −10.30%**).

어제 「조각 뒤처리 필터」로 세 세기 모두 지고 되돌린 도구입니다. 진 이유가 필터가 아니라 **심판**이었습니다. 그 실험은 08-21 16:08 이고 지각 지표는 VMAF 가 21:21, SSIMULACRA2 가 23:16 에 붙었습니다. 평활 필터를 가장 못 보는 두 자로만 재고 버린 겁니다. 그리고 그때 필터는 평탄 판정도 모서리 판정도 없는 단일 세기였습니다.

HEVC 식으로 다시 짰습니다. 경계 양쪽이 이미 무늬면 두고 가고, 차이가 크면 진짜 모서리로 보고 안 건드립니다. **비트를 한 바이트도 안 씁니다** — 같은 파일이고 디코더만 다릅니다.

우리 결함이 실제로 블록이라는 것도 쟀습니다. 16 격자 위의 차이가 원본 대비 품질 20 에서 +133%, 품질 63 에서 +571% 초과였습니다.

호환은 머리말의 예약 비트로 잡습니다. 규격이 예약 자리를 「1 이면 거절」로 정해 둔 것이 정확히 이 자리를 위해서였고, 처음 쓴 사례입니다.

실물로 양쪽을 확인했습니다. 새 파일을 v1.6 디코더에 주면 `reserved bit set` 으로 거절되고, v1.6 파일을 새 디코더에 주면 `malformed group data` 로 거절됩니다. **양쪽 다 거절이고 조용한 오답은 없습니다.** 뒤쪽이 거절되는 것은 필터 때문이 아니라 같은 판에서 계수 부호화가 함께 바뀌었기 때문입니다 — 예약 비트가 주는 「옛 파일을 그대로 읽는」 성질은 필터만 바꾼 판에서 쓸 수 있는 것이고, 이번 판은 그런 판이 아닙니다.

**5. 시작 확률표를 다시 배웠습니다.**

깃발 0 이 보는 표본이 바뀌었으므로 `tools/learn_probs.py` 로 다시 만들었습니다. 위 3번의 −4.85 / −4.27 / −5.29 는 깃발 건너뛰기(−4.72 / −4.23 / −5.20)에 이 재학습까지 더한 값입니다.

**재서 진 것도 적습니다.** 남겨 두지 않으면 다음에 같은 길을 다시 팝니다.

- **계수 정밀도를 비트와 맞바꾸는 갈래를 닫았습니다.** 꼬리 계수 자르기(두 번), 블록마다 양자화 세기 조절(두 번), 블록마다 λ 조절, 계수 크기 율왜곡 낮추기 — 여섯 번 재고 여섯 번 다 SSIMULACRA2 가 무너졌습니다. PSNR 은 좋아지는 경우가 있어 제곱 오차만 보면 이득으로 보입니다.
- **부호에 문맥 모델**: 부호가 전체 비트의 12~18% 인데 전부 반반으로 나갑니다. 대역·평면·이웃 부호·크기까지 넣어 재니 최대 0.55% 였습니다. 값이 안 됩니다.
- **평면 밖 블록에 아무것도 안 적기**: 사실상 0 이었습니다. 이유가 재미있는데, **분할 자체가 거의 안 일어납니다** — 품질 20 에서 16×16 단위 15,486 개 중 나뉜 것이 10 개, 품질 63 은 0 개입니다.
- **구별되지 않는 모드에 비트 안 내기**: 조각의 맨 위 줄에서는 세로·평면이 DC 와 같은 그림이라 넷 중 둘만 서로 다릅니다. 그 자리를 1비트·0비트로 줄여 보았고, 양쪽 다 확률표를 다시 배워 견주니 **켠 판 −7.16 / −7.68 / −10.36, 끈 판 −7.18 / −7.68 / −10.30** 으로 동률이었습니다. 옛 방식이 낭비하던 게 아니었어요 — 같은 그림을 내는 모드가 여럿이면 인코더가 그중 가장 싼 것을 고를 수 있고, 그 자유가 줄여서 얻는 만큼을 이미 벌고 있었습니다.
- **데드존 재훑기**: 7 도 8 도 6 보다 나쁩니다. 양쪽이 다 나빠 6 이 굳었습니다.
- **「이웃 화소를 다듬는 쪽에 답이 있을 것이다」**: 08-21 이력에 적어 둔 제 추측인데, 만들어 재니 문턱 4·8·16 전부에서 집니다. 답은 예측에 먹이는 평활이 아니라 출력에 거는 필터였습니다.
- **방향 예측과 예측 모드에 맞는 변환(ADST)을 같이 넣기**: 이력의 두 기각이 서로를 이유로 대고 있었습니다. ADST 는 「우리는 모드가 넷뿐이라 쓸 자리가 적다」(08-21), 방향 예측은 「자를 고치고 확률표까지 양쪽 재학습해도 진다」(08-23, 여섯 번째)입니다. A 는 B 가 없어서 지고 B 는 A 가 없어서 진다고 적혀 있으니, 하나만 넣는 시험으로는 이 고리를 못 끊습니다. 그래서 네 조합을 **각자 확률표를 다시 배워** 한 자로 쟀습니다. 대조군 기준으로 방향만 **+1.45 / +2.64 / +0.39**, 변환만 **+1.70 / +2.41 / +2.73**, **둘 다 +5.25 / +7.81 / +5.11** 입니다. 같이 넣으면 손해가 서로 갚아지는 게 아니라 **더해집니다.** 배정을 가장 얌전하게 잡아도(세로·가로 예측에만 ADST) 방향만 +1.45 에 변환만 +1.12 이고 둘 다가 +2.58 로, 거의 정확히 두 손해의 합입니다. **순환 논리는 이걸로 닫습니다.**
- **덤으로 나온 것**: 방향을 일부러 뒤집어 붙인 판(세로 예측에 가로 ADST)이 제대로 붙인 판보다 **덜 집니다** — 여덟 장 중 일곱 장에서 PSNR 0.35%p 앞섭니다. 변환 자체는 제대로 붙었습니다(모의 잔차로 확인: 위에서 0 으로 출발하는 무작위 걸음에 세로 ADST 를 걸면 문턱 넘는 계수가 5.38 → 5.13 개로 줍니다). 그러니 이건 배정 실수가 아니라, **우리 잔차에서는 ADST 가 이론이 말하는 일을 안 하고 있다는 두 번째 증거**로 읽습니다. DCT 가 잘하던 축에 걸면 더 잃고, 이미 못 담던 축에 걸면 덜 잃을 뿐이에요.

**최종 성적** (표준 사진 8장, 음수면 우리가 비트를 덜 씁니다)

| 상대 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| JPEG | **−31.6%** | **−30.8%** | **−21.8%** |
| WebP | **−14.5%** | **−6.2%** | **−11.0%** |
| JPEG XL | **−10.9%** | +5.3% | +17.7% |
| AVIF | +45.8% | +42.2% | +18.2% |

우리 소재 12장에서는 JPEG **−45.3 / −37.8 / −36.7%**, WebP **−22.3 / −17.4 / −8.3%**, JPEG XL **−30.5 / −17.8%** / +22.2%, AVIF +82.8 / +93.3 / **+56.2%** 입니다.

**AVIF 와의 지각 격차가 +32.8% 에서 +18.2% 로 줄었습니다.** 우리 소재에서도 +70.7% 에서 +56.2% 입니다. 아직 지고 있고, 숨기지 않습니다.

**속도.** 디코딩 0.178 → **0.171 초**입니다. 필터를 붙였는데 빨라졌습니다 — 답이 정해진 깃발을 안 읽게 되면서 아낀 것이 필터 비용보다 컸습니다. 그래도 다섯 중 꼴찌입니다(JPEG 0.063, WebP 0.079, AVIF 0.085, JXL 0.088). 인코딩은 1.64 → **2.74 초**로 늘었고, 네 모드를 다 담아 보는 대가입니다. AVIF 의 6.41 초보다는 빠릅니다.

**줄 수.** `src/` 2,475 → **2,784 줄**, 디코딩 경로 1,631 → **1,900 줄**입니다. 필터 파일 103 줄과 부호기 변경이 들어갔습니다.

## v1.6.0 — 2026-08-22

**라이선스를 Apache License 2.0 으로 바꿉니다.** 몇 시간 전 v1.5.0 에서 GPL-3.0 으로 갔다가 되돌리는 것이라, 왜 갔고 왜 왔는지를 적어 둡니다.

**목적**은 그때나 지금이나 하나입니다 — 상용 제품이 써도 되지만 만든 사람은 반드시 밝히게 하는 것.

**GPL 이 그 일을 못 합니다.** 조항을 읽어 확인했습니다.

- GPLv3 4조는 "source code" 에 대한 조항이고, **바이너리만 배포할 때의 표시 장소를 정한 문장이 GPL 에 없습니다.** 6조가 대응 소스를 같이 주라고 요구하기 때문에 그럴 일을 상정하지 않습니다.
- 화면 표시를 다루는 5조 d항에는 면제가 붙어 있습니다. 원본이 저작권을 안 띄우면 수정본도 안 띄워도 됩니다. `tools/cli.c` 의 사용법 출력에 저작권 줄이 없으므로 이 면제에 걸립니다.
- GPL 이 실제로 강제하는 것은 표시가 아니라 소스 공개입니다(5조 c항·6조). 링크하는 제품 전체가 대상입니다.

**Apache 2.0 이 그 일을 합니다.** 4조 d항이 재배포물에 NOTICE 파일의 내용을 실으라고 요구하고, 6조가 그 재현을 상표 조항의 예외로 못박습니다. 표준 라이선스 중 **저작권자가 표시할 문장을 직접 정해 아래로 내려보낼 수 있는 것은 이것뿐**입니다. MIT·BSD 는 내려가는 것이 저작권 한 줄로 고정입니다.

`NOTICE` 파일을 새로 넣었습니다. 이름과 저장소 주소 두 줄입니다.

**덤으로 확인한 것** — "ffmpeg 도 GPL 인데 다들 쓴다" 는 관찰은 절반만 맞습니다. FFmpeg 프로젝트 자체는 LGPL 2.1+ 이고 GPL 부품은 `--enable-gpl` 로 켜야 들어갑니다. 크롬이 출시하는 설정은 `CONFIG_GPL 0` 입니다. 상용 제품은 GPL 부품을 뺀 빌드를 씁니다.

**BSD 2-Clause(v1.4 까지)도 이미 표시를 강제하고 있었습니다.** 2항이 바이너리 재배포에도 저작권 고지를 문서에 재현하라고 요구합니다. Apache 로 간 이유는 그 위에 **표시할 문장을 직접 쓸 수 있다**는 것 하나입니다.

코드와 비트스트림은 그대로입니다. 줄 수만 2,484 → 2,475 로 줄었는데 고지가 16 줄에서 15 줄로 짧아졌기 때문입니다.

**법률 자문이 아닙니다.** 라이선스 원문을 읽고 판단한 것이고, 실제로 분쟁을 다툴 자리라면 변호사 확인이 필요합니다.

## v1.5.0 — 2026-08-22

**라이선스를 BSD 2-Clause 에서 GPL-3.0-or-later 로 바꿉니다** (영웅님 지시). 전문은 gnu.org 에서 받은 674 줄 원본이고, 소스 열 개에 고지를 붙였습니다.

코드와 비트스트림은 그대로입니다. 줄 수만 2,312 → 2,484 로 늘었는데 그중 160 줄이 고지입니다.

**알아 두실 것**: 널리 쓰이는 코덱 라이브러리는 전부 허용적 라이선스입니다(libjpeg·libwebp·libaom·libjxl 모두 BSD 계열). GPL 이면 이 라이브러리를 링크하는 프로그램도 GPL 이 되어야 하므로 상용 채택이 크게 줄어듭니다. 라이브러리를 그대로 쓰게 하면서 수정분만 공개시키려면 LGPL 이 맞습니다.

## v1.4.0 — 2026-08-22

**"완성"을 목표로 받고 전수 검사를 돌린 판입니다.** 스무 부품을 검사표로 고정하고 하나씩 채점했더니 일곱이 불합격이었습니다. 문법이 바뀌므로 v1.3 파일은 읽히지 않습니다.

**가장 큰 것: 시험이 아무것도 안 지키고 있었습니다**

- 왕복 시험이 PPM 머리의 크기 두 개만 보고 **화소를 한 개도 안 봤습니다.** 디코더가 잔차를 통째로 버려 8.9 dB 를 내게 해도, 색차를 뒤바꿔 20.3 dB 를 내게 해도 네 시험이 전부 통과했습니다.
- 거절 규칙 열 가지 중 **여섯이 한 번도 안 돌았습니다.** 지워도 시험이 초록이었습니다.
- 망가진 파일 300건 중 **44건이 조용히 딴 그림을 냈습니다.** 40건이 20 dB 아래, 최악 11.55 dB 였는데 시험은 "거절"과 똑같이 통과로 셌습니다.

**고친 것**

- 머리말의 **예약 32비트를 손상 감지 해시로** 바꿨습니다. 이미 0 으로 비어 있던 자리라 **크기가 한 바이트도 안 늘고** 조용한 오답이 0 건이 됐습니다.
- 화소 버퍼를 잡기 전에 목차를 검증합니다. 1KB 짜리 파일이 600MB 를 물리던 것을 막습니다.
- 왕복 시험이 화소를 확인하고 경우를 6 개에서 11 개로 늘렸습니다(4:2:0·품질 양 끝·조각 양 끝 포함). 골든에 디코딩 결과까지 잠갔습니다.
- 거절 규칙마다 그것만 어긋난 파일을 만드는 `test_reject` 를 넣었습니다 — 10/10 통과합니다.
- **규격서를 코드에 맞췄습니다(아홉 자리).** 역변환 행렬 방향, 시프트의 음수 반올림, 평면 예측 기울기 눈금, 이웃 개수, 시작 확률, `last` 상한, 모드 문맥, 모델 총수, 무리 산식입니다. 그전에는 남이 규격서만 보고 만든 디코더가 우리 파일을 한 장도 못 읽었습니다.
- 확률표를 `tools/learn_probs.py` 로 재현 가능하게 만들었습니다. 같은 이미지·같은 품질로 두 바퀴 돌리면 같은 표가 나옵니다.
- 공개 헤더를 영어로 다시 썼습니다. 첫 줄이 아직 `codec.h` 였고 "이름이 정해지면 바꾼다" 는 문장이 남아 있었습니다.

**압축률은 v1.3 과 같습니다** — 해시가 빈자리를 쓰고 나머지는 문서·시험 수정이라 비트가 안 늘었습니다.

| 상대 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| JPEG | **−28.4%** | **−26.3%** | **−17.7%** |
| WebP | **−11.4%** | **−2.3%** | **−6.8%** |
| JPEG XL | **−6.6%** | +11.0% | +23.7% |
| AVIF | +59.1% | +59.9% | +32.8% |

우리가 만드는 소재 12장: JPEG **−41.8 / −34.7 / −32.3%**, WebP **−17.3 / −14.2 / −2.7%**, JPEG XL **−26.4 / −14.0%** / +29.5%, AVIF +96.7 / +104.8 / +70.7%.

## v1.3.0 — 2026-08-22

계수와 모드를 담는 방식을 세 군데 손봤습니다. 문법이 바뀌므로 v1.2 파일은 읽히지 않습니다.

**바꾼 것 셋**

- **계수 무리를 2차원 이웃으로.** 지그재그 순서에서 바로 앞 두 개를 보던 것을, 블록 안 자리 기준으로 왼쪽·위·왼쪽위 셋을 보게 바꿨습니다. 지그재그는 대각선을 번갈아 훑으므로 순서상 앞이라고 블록 안에서 옆인 것이 아닙니다. 이웃 표본이 세 배로 늘어 단계도 셋에서 다섯으로 늘렸습니다.
- **시작 확률표를 학습해서 깔았습니다.** 조각마다 확률이 반반에서 출발해 조각이 끝날 때쯤 자리를 잡고, 다음 조각에서 다시 반반으로 돌아가고 있었습니다. 표준 사진 여덟 장을 담아 조각 끝의 확률을 모아 평균했습니다. 표는 1.4KB 입니다.
- **모드 비트에 앞 블록의 모드를 문맥으로.** 이웃한 블록은 대개 같은 방향으로 예측됩니다.

**이른 곳** — AOM 공통 시험 집합 8장

| 상대 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| JPEG | **−28.4%** | **−26.3%** | **−17.7%** |
| WebP | **−11.4%** | **−2.3%** | **−6.8%** |
| JPEG XL | **−6.6%** | +11.0% | +23.7% |
| AVIF | +59.1% | +59.9% | **+32.8%** |

우리가 만드는 소재 12장: JPEG **−41.8 / −34.7 / −32.3%**, WebP **−17.3 / −14.2 / −2.7%**, JPEG XL **−26.4 / −14.0%** / +29.5%, AVIF +96.7 / +104.8 / +70.7%.

**v1.1 대비** — 오늘 하루 AVIF 와의 지각 지표 격차가 **+43.5% 에서 +32.8% 로 10.7 포인트** 줄었습니다. 디코딩 경로는 1,303 줄에서 1,445 줄이 됐습니다.

**부수 효과** — 확률표가 조각 독립의 대가도 줄였습니다. 조각 64 에서 29.7% 였던 것이 22.3% 입니다. (**08-23 정정**: 그 뒤 부호기가 크게 바뀌어 이 개선이 남아 있지 않습니다. 다시 재니 64 가 +29.81%, 256 이 +2.86% 입니다.)

**되돌린 것 셋** — 블록마다 양자화 세기 조절(지각 지표가 나빠짐), 계수 꼬리 자르기(같은 이유, 두 번째 시도), 나눔 비트에 이웃 문맥(전 칸 미세하게 나빠짐). 이유는 `SPEC.md` 의 문법 변경 이력에 적었습니다.

## v1.2.0 — 2026-08-22

**인코더의 자가 세 곳이나 고장나 있었습니다.** 그것을 고치고, 계수를 담는 방식을 두 군데 바꿨습니다. 문법이 바뀌므로 v1.1 파일은 읽히지 않습니다.

**고친 자 셋** (전부 디코더 0줄)

- 모드 비트와 나눔 비트의 값을 실제의 **1/16** 로 매기고 있었습니다. 인코더가 블록 쪼개는 비용을 거의 공짜로 보고 있었습니다.
- λ 를 정수로 들고 다녀 **고품질에서 0 으로 뭉개졌습니다.** q1 이면 (3×3×10)/1600 = 0 입니다. 그 구간에서 율왜곡 판단이 통째로 죽어 있었습니다.
- 왜곡 자가 블록 하나만 보아 **예측 오차 전파를 못 봤습니다.** 블록의 끝 열·끝 행은 다음 블록의 이웃이 되므로 32/16 배로 셉니다.

**바꾼 문법 둘**

- **계수 크기 꼬리에 확률 모델**을 붙였습니다. 그 전에는 전체 비트의 38.8% 가 모델 없이 반반으로 나가고 있었습니다. 확률이 반반인 자리는 한 비트도 못 아낍니다.
- **계수 무리를 2차원 이웃으로** 바꿨습니다. 지그재그 순서의 앞 두 개가 아니라 블록 안 자리 기준 왼쪽·위·왼쪽위 셋을 봅니다. 지그재그는 대각선을 꺾는 자리에서 먼 계수를 이웃이라 부르고 있었습니다.

**이른 곳** — AOM 공통 시험 집합 8장

| 상대 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| JPEG | **−27.5%** | **−25.4%** | **−16.7%** |
| WebP | **−10.7%** | **−1.5%** | **−5.8%** |
| JPEG XL | **−5.5%** | +12.2% | +25.2% |
| AVIF | +62.2% | +63.1% | +36.1% |

우리가 만드는 소재 12장에서는 JPEG(**−41.1 / −34.1 / −31.3%**)과 WebP(**−16.4 / −13.6 / −1.3%**)를 세 지표 모두에서 앞섭니다.

**v1.1 대비** — AVIF 와의 지각 지표 격차가 +43.5% 에서 **+36.1%** 로 7.4 포인트 줄었습니다.

**함께 고친 것 둘**

- 규격서의 역양자화 절이 코드와 달랐습니다. 곱하기 하나로만 적혀 있었는데 디코더는 자리 가중과 색차 보정을 씁니다. **규격서만 보고 만든 디코더는 우리 파일을 한 장도 못 읽었습니다.**
- 역변환 누산이 32비트였습니다. 손상된 파일이 최대 계수를 실으면 한계의 31.6 배까지 갑니다. 64비트로 바꿨습니다.

**대가** — 디코딩이 0.096 초에서 **0.178 초**로 느려졌습니다. 다섯 중 꼴찌였는데 더 벌어졌습니다. 인코딩도 1.16 초에서 1.64 초가 됐습니다. 압축률을 사면서 속도를 팔았고, 그 값을 아직 안 치렀습니다.

## v1.1.0 — 2026-08-21

**규격 상수 하나가 바뀝니다.** v1.0.0 파일은 v1.1 디코더로 읽히지 않습니다. 낸 지 몇 시간 만의 변경이라 채택자가 없고, 바꿀 이유가 충분해서 미루지 않았습니다.

**무엇을 알게 되었나** — 벤치에 지각 지표(SSIMULACRA2)를 붙였습니다. 그러자 v1.0 설정이 **JPEG 에게조차 지각 지표로 지고 있었다는 것**이 드러났습니다(+5.6%). PSNR 로는 WebP·JPEG XL 까지 앞서는 것처럼 보이던 판이었습니다. 제곱 오차 하나만 보고 다듬어 온 대가입니다.

**무엇을 바꿨나** — 높은 자리 계수를 얼마나 더 거칠게 양자화할지 정하는 상수(`INGOT_QW_ALPHA`)를 0 에서 1 로 올렸습니다. 사람 눈은 고주파 오차를 덜 봅니다. 세 지표 모두에서 JPEG 을 이기는 가장 작은 값입니다.

| 상대 | PSNR | SSIM | SSIMULACRA2 |
|---|---|---|---|
| JPEG | **−23.3%** | **−21.1%** | **−11.3%** |
| WebP | **−4.4%** | +5.4% | +1.3% |
| JPEG XL | +0.1% | +19.0% | +34.0% |
| AVIF | +70.6% | +70.5% | +43.5% |

우리가 만드는 소재 12장: JPEG **−35.2% / −26.9% / −26.9%**, WebP −8.5% / −3.4% / +4.7%, JPEG XL −17.6% / −0.9% / +40.6%.

**대가** — PSNR 이 −25.7% 에서 −23.3% 로 물러섰습니다. JPEG XL 과의 PSNR 우위(v1.0 의 −3.3%)를 내주고 동률이 됐습니다. 세 지표를 함께 보면 그것이 옳은 거래였습니다.

**바꾸지 않은 것** — 가중치 2 로 더 가면 SSIMULACRA2 가 −16.4% 까지 좋아지지만 PSNR 로 WebP 와 JPEG XL 을 모두 잃습니다. 한 지표를 위해 나머지를 내주지 않았습니다.

## v1.0.0 — 2026-08-21

포맷을 고정합니다. 여기서부터 `INGT` 비트스트림은 바뀌지 않습니다. 뒤에 무엇을 더하든 새 프로파일로 나가고, v1 파일은 계속 읽힙니다.

**고정 시점의 자리** — AOM 공통 시험 집합 8장, 품질 여섯 단계

| 상대 | PSNR | SSIM | VMAF |
|---|---|---|---|
| JPEG | **−25.7%** | **−21.0%** | **−3.2%** |
| WebP | **−9.1%** | +2.2% | +33.9% |
| JPEG XL | **−3.3%** | +19.4% | +42.4% |
| AVIF | +68.4% | +74.7% | +72.6% |

우리가 만드는 소재 12장에서는 JPEG 을 세 지표 모두에서(**−39.9% / −27.7% / −39.1%**), JPEG XL 을 셋 중 둘에서 앞섭니다.

**이번에 더한 것** — 벤치의 셋째 축으로 지각 지표(VMAF)를 붙였습니다. SSIMULACRA2 는 실행 파일 배포본이 없고 소스 빌드가 libjxl 전체를 요구해 쓰지 못했습니다.

**그래서 알게 된 것** — PSNR 로 앞선 WebP·JPEG XL 에 지각 지표로는 크게 집니다. 제곱 오차만 보고 판단해 온 인코더의 대가입니다. 고주파 양자화 가중치를 1 로 올리면 VMAF 가 JPEG 대비 −3.2%에서 −15.1%로 좋아지지만 PSNR 이 −25.7%에서 −23.3%로 물러섭니다. 한 지표만 보고 정할 수 없어 v1 에서는 보류하고, SSIMULACRA2 로 교차 확인한 뒤 정하기로 했습니다.

**남은 약점** — 인코딩이 1.16초로 JPEG(0.081초)보다 14배 느립니다. AVIF 는 압축률에서 68% 앞서 있습니다.

## 2026-08-21 (둘째 판)

**바꾼 것**

- 골롬-라이스를 **이진 산술 부호화**(레인지 코더)로 교체했습니다. 자리마다 확률 모델을 두고, 인코더는 1/16 비트 단위 값 표로 비용을 잽니다.
- 계수 무리에 **직전 계수의 크기**를 넣었습니다. 무리는 8개에서 30개가 됐습니다.
- 양자화 반올림 자리를 절반에서 **5/16** 으로 당겼습니다(데드존). 규격이 아니라 인코더의 선택이라, 옛 파일이 그대로 읽힙니다.
- **4×4 블록**을 넣어 분할을 16 → 8 → 4 재귀 구조로 바꿨습니다.
- 블록 머리말(`last`) 무리를 **블록 크기별로** 갈랐습니다.
- 인코딩·디코딩 시간을 재는 `tools/speed.py` 를 더했습니다.

**이른 곳** — AOM 공통 시험 세트 8장, 품질 여섯 단계, 기계 한 대

| 상대 | PSNR BD-rate | SSIM BD-rate |
|---|---|---|
| JPEG | **−25.6%** | **−21.0%** |
| WebP | **−9.0%** | +2.4% |
| JPEG XL | **−3.2%** | +19.5% |
| AVIF | +68.6% | +74.8% |

우리가 만드는 소재 12장에서는 JPEG(−39.5%/−27.2%)과 JPEG XL(−23.1%/−1.9%)을 두 지표 모두에서 앞섰고, WebP 는 PSNR 로 −15.5% 앞섰습니다.

**되돌린 것** — 블록 경계 필터, 꼬리 계수 자르기, 예측 모드 여덟(두 번), 블록별 반올림 조절, 고주파 양자화 가중치. 이유는 `SPEC.md` 의 문법 변경 이력에 적었습니다.

**인코더를 빠르게** — 모드를 잔차 절대합으로 먼저 걸러 둘만 담아 보고, 통째로 담는 값이 24비트보다 싸면 나누기를 아예 재지 않습니다. 인코딩이 2.67초에서 **1.16초**로 줄었고, 뜻밖에 압축률도 함께 좋아졌습니다 — 우리 판단이 분할을 과하게 골라 왔다는 뜻입니다. 조각마다 쓸 자리를 따로 잡아 두어 병렬 인코딩이 가능해졌지만, 이 기계에 OpenMP 런타임이 없어 병렬 속도는 재지 못했습니다.

**남은 약점** — 인코딩이 1.16초로 JPEG(0.081초)보다 14배 느립니다. AVIF 는 압축률에서 아직 69% 앞서 있고, SSIM 으로는 표준 사진에서 WebP(+2.4%)와 JXL(+19.5%)에 집니다.

## 공개 전 (2026-08-21)

**만든 것**

- 정수 8×8 DCT, 균일 양자화, 지그재그, 조각 독립 구조와 목차
- 적응형 골롬-라이스 부호화 (자리·평면별 무리 8개)
- DC 예측 (조각·평면 안에서 앞 블록과의 차이)
- 색차 줄이기(4:2:0) 옵션
- 시험 장치: 결정성·왕복·고정 결과·손상 입력 300건
- 측정 장치: BD-rate (PSNR·SSIM, 단조 보존 보간), 기존 포맷 비교, 상수 훑기

**잰 것** — AOM 공통 시험 세트 8장, 품질 여섯 단계, 기계 한 대

| 바꾼 것 | PSNR | SSIM |
|---|---|---|
| DC 예측 | −9.4% | — |
| 자리·평면별 양자화 가중 | −0.6% | −1.5% |
| 적응형 골롬-라이스 | 누적 −11.7% | 누적 −10.4% |
| 색차 줄이기(옵션) | −10.2% | −13.2% |

**되돌린 것**

- 0의 연속을 개수로 묶기: PSNR +6.2%, SSIM +6.6%로 나빠져 되돌렸습니다.

**고친 것**

- 조각이 이미지 밖으로 나가도 통째로 인코딩하던 결함. 조각 1024에서 파일이 76% 컸습니다.
- BD-rate 계산이 SSIM 곡선에서 발산하던 문제. 3차 다항식 전역 적합을 단조 보존 보간으로 바꿨습니다.
- 런렝스의 무리 번호가 인코더와 디코더에서 어긋나던 문제.

### 같은 날 이어서

- **인트라 예측 4모드** (DC·세로·가로·평면). 인코더가 디코더처럼 복원하도록 고쳤습니다. **PSNR −34.4%, SSIM −21.0%**
- 양자화 범위를 1~127에서 1~375로 넓혔습니다. 그 전에는 낮은 비트레이트에 못 닿아 BD-rate를 절반도 못 쟀습니다
- 블록 머리말을 고정 7비트에서 적응 부호화로 바꿨습니다. 같은 화질에서 파일이 q40에서 49%, q63에서 58% 줄었습니다
- **버그 수정**: 이웃 화소가 평면 오른쪽·아래 경계를 넘어가던 것. 손상 입력 시험이 잡았고, 정상 입력에서도 가장자리 블록의 예측이 틀렸습니다

**여기까지의 성적** — AOM 공통 시험 세트 8장, PSNR BD-rate

| 상대 | 4:4:4 | 4:2:0 |
|---|---|---|
| JPEG | +7.4% | **+1.3%** |
| WebP | +20.1% | +15.2% |
| JPEG XL | +39.3% | +33.5% |
| AVIF | +147.7% | +159.3% |

### 가변 블록 (2026-08-21)

- **16×16 묶음마다 통째로 담을지 8×8 넷으로 나눌지 고릅니다.** 두 후보를 다 시험 인코딩해 실제 비트 수와 왜곡을 견주고 싼 쪽을 씁니다
- 예측 모드 선택도 잔차 절대합에서 같은 기준으로 바꿨습니다
- **여기서 JPEG을 넘었습니다** — PSNR BD-rate −1.0%(표준 사진), −7.7%(우리 소재)
- 색차 줄이기는 이제 이득이 없습니다. 16×16 블록이 평탄한 색차를 이미 잘 담기 때문입니다

**성적** — PSNR BD-rate, 양수는 우리가 비트를 더 씀

| 상대 | 표준 사진 8장 | 우리 소재 12장 |
|---|---|---|
| JPEG | **−1.0%** | **−7.7%** |
| WebP | +16.9% | +32.6% |
| JPEG XL | +30.9% | +13.3% |
| AVIF | +137.7% | +228.3% |

SSIM 기준으로는 아직 JPEG에도 뒤집니다(+7.9% / +8.4%). 두 지표가 갈리는 것을 그대로 적습니다.
